# Python bytecode
__pycache__/
*.py[codz]
*$py.class
*.so

# Distribution / packaging (Python wheels, eggs — NOT frontend/dist)
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Type checkers & linters
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/
cython_debug/

# IDE / editor
#.idea/
# .vscode/
.cursorignore
.cursorindexingignore
.spyderproject
.spyproject
.ropeproject

# Package managers (lock files intentionally tracked)
.pdm-python
.pdm-build/
.pixi
__pypackages__/

# Jupyter
.ipynb_checkpoints

# Frontend
frontend/node_modules/
# frontend/dist/ is a build artifact — NOT tracked. Build before serving
# (serve.ps1 runs `npm run build`). FastAPI serves it at /dashboard from disk.
frontend/dist/

# Local databases (runtime state, not version-controlled)
*.db
*.db-shm
*.db-wal
app/ml/models/feature_store.db
app/ml/models/price_cache/

# Training data & model artifacts
*.parquet
app/ml/models/*.pkl
app/ml/models/*.joblib

# Runtime logs (app server, training runs, backtests — not version-controlled)
logs/
*.log

# Backtest & training result artifacts
results/
!results/.gitkeep

# Claude Code internal state
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/state/model_meta_cache.json
.claude/state/overnight_jobs.json
.claude/state/last_morning_cursor.txt
.claude/worktrees/

# MCP config (may contain API keys)
.mcp.json

# Large generated log files — don't commit
docs/phase_h_plus_rerun_log.txt
docs/archive/data/phase_h_plus_rerun_log.txt

# Alpha-v7 review request pack — local convenience copies (canonical files live in repo)
docs/reference/prompts/20260612_AlphaV7_Request/
