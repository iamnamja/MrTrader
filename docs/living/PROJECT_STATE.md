# MrTrader — Project State

**One-screen view of what's happening right now. Updated at session start/end when focus changes.**

> **Update rule:** Human updates this at session boundaries. Keep it to one screen. This is NOT a planning doc (that's MASTER_BACKLOG.md) and NOT a history doc (that's ML_EXPERIMENT_LOG.md). It answers: "If I open the laptop cold, what do I need to know in 30 seconds?"

**Last updated:** 2026-06-12 (**ALPHA-v7 Phase B / Ruler v2 — Phase 1 (PR #471) + Phase 2 (PR #472) LANDED, both DARK. Phase 2 = `bayes_sr.py` (Bayesian posterior P(SR>0), replaces saturated DSR) + `ruler_v2.py` (two-tier gate) + `CPCVResult.oos_returns_dated` + `GATE_MODE="ruler_v2"` dispatch; legacy gates byte-for-byte untouched (89 tests). Opus deep-dive caught a CRITICAL: CAPITAL was "unreachable on backtest alone" only by threshold luck → made live-paper a STRUCTURAL gating criterion (posterior = P(SR>0 | backtest AND live paper)). No live behavior change (flag not flipped; owner OD-1…OD-9 sign-off pending). Earlier today: H1 RUN → PEAD DEMOTED at event level (p=0.78). ✅ PEAD FLIPPED OFF LIVE + uvicorn restarted → live book = trend-only (25%) + cash. P0+P1c+P2+P3-H1 shipped (#454/#455/#456) + P4a options feature table + H4a–H4e pre-registered. P4 H4a–H4e → ALL 5 KILL; H2 NOT_CONFIRMED (OPT-5 parked); H3 BLOCKED (revision data). All Alpha-v6 hypotheses adjudicated (P5 PARK). NEW DIRECTION: ALPHA-v7 — operate a premia book (`docs/reference/ALPHA_V7_SYNTHESIS_AND_PLAN.md`; Phase B design = `docs/reference/RULER_V2_DESIGN.md`). Live book unchanged.**)

## 🧭 NOW (2026-07-08): Compound-and-Harden ✅ COMPLETE through CH4 — CH0 baseline+scorecard, CH1 per-name gate (shadow), CH2 antifragile sizing (all 3 KILLED), CH3 regime diagnostic (parked strategies closed; crash governor kept), CH4 ranging-MR search **KILLED → 12-mo hunting MORATORIUM in effect (to 2027-07-08)**. Verdict: edge = trend+cash, operate it simply. **NOW: CH5** (live accrual; review 2027-07-08). IBKR R1 migration surface BUILT + cutover-hardened (inert). No live change in CH0–CH4.

**🧭 STRATEGIC DIRECTION SET (2026-07-07, 5-LLM Comprehensive Review — SSOT `docs/reference/prompts/20260707_Comp_Review/SYNTHESIS_AND_DECISION.md`):** unanimous verdict — **STOP the broad second-edge hunt; COMPOUND-AND-HARDEN the one edge (ETF trend + cash).** The null is the market not the method; every real thing is null / collinear-with-trend / cost-decay-killed. **Ranked plan:** (1) close the per-name correlation/heat gap on the live path [gross/beta/notional/recon already ENFORCE — hardening, not an emergency]; (2) **make trend antifragile** — 3 NEW pieces (trend-strength gross, correlation-regime gross, trending-vs-whipsaw governor), gated to beat constant-gross trend OOS; (3) live-forward scorecard; (4) regime-conditional decomposition as a *diagnostic*; (5) ONE terminating pre-registered search (ranging-market MR, complement-of-trend regime filter, detection-lag test, written 12-mo moratorium if it fails). **DON'T:** buy alpha data, build a regime→selection layer, resurrect the futures book (marginal + cost-inflated even at full breadth per FB0), hard regime switches. **Futures-live = SHELVED (FB-SHELVE).** See DECISIONS 2026-07-07. **→ Phased execution = the Compound-and-Harden Program (SSOT [`docs/reference/COMPOUND_AND_HARDEN_PROGRAM_2026-07-07.md`](../reference/COMPOUND_AND_HARDEN_PROGRAM_2026-07-07.md)): CH0 baseline+scorecard → CH1 correlation/heat gate → CH2 antifragile sizing → CH3 regime diagnostic → CH4 one MR search → CH5 accrual. NOW: CH0. ✅ **CH0a DONE (2026-07-07):** constant-gross ETF-trend baseline FROZEN (`scripts/ch0_baseline.py` → `docs/reference/ch0_trend_baseline.json`; pinned `end=2026-07-07` + data fingerprint for immutability; Opus-reviewed, no CRITICAL). The CH2 bar = CPCV mean_sharpe **0.7009** + regime profile **BULL +4.11 / NEUTRAL +0.44 / BEAR −0.77** → CH2's gate is DUAL: beat mean_sharpe OOS AND don't regress BEAR (folds under-sample stress). ✅ **CH0b DONE (2026-07-07):** live-forward scorecard EXTENDED (`app/live_trading/back_validation.py` + `trend_sleeve.py`) — persists each individual governor multiplier (crash/credit/ladder) + the ungoverned counterfactual book at every live rebalance; report adds the static-vs-governed counterfactual (`governor_pnl`) + regime-conditional attribution (same BULL/NEUTRAL/BEAR taxonomy as CH0a). Opus-reviewed, provably inert to live trades. **Live counterfactual accrues weekly from the Mon 07-13 enforce rebalance (can't be backfilled → landed first).** ✅ **CH1 SHADOW-LANDED (2026-07-07):** per-name correlation/heat/concentration gate on the live trend path (`app/live_trading/per_name_gate.py`, mirrors the whole-book gate; `pm.per_name_gate_mode` default **shadow**; wired into `run_trend_rebalance` after the whole-book gate). Book-level metrics off already-fetched data (no new I/O): correlation = NAV-weighted signed avg pairwise book corr (COARSE "one bet" backstop, threshold 0.90/0.95 HIGH above the structural ~0.85 equity-ETF corr so it doesn't hold normal weeks); concentration = per-name cap max(0.25, configured max_pos); heat vs 6%. Deliberately did NOT reuse the RM sector check (all-ETF → UNKNOWN → would breach weekly). Opus-reviewed, provably inert to live trades. **⚠️ BEFORE ENFORCE: the correlation threshold is PROVISIONAL — the gate records `weighted_avg_book_corr` weekly; calibrate the enforce threshold from the shadow-soak distribution first.** **CH0 + CH1 COMPLETE.** 🔄 **CH2 IN PROGRESS (2026-07-08):** shared gate harness built (`app/research/ch2_sizing.py` — governed book × m[t] through the same CPCV path CH0a was frozen on; DUAL gate). ❌ **CH2b correlation-regime gross KILLED** — all 3 pre-registered configs FAIL every prong OOS (primary mean_sharpe 0.6607 vs 0.7009, improvement not significant, BEAR regresses); high held-book correlation is predominantly a strong equity-BULL signal (trend's best periods), not a crisis signal → cutting there sacrifices return (correlation is direction-blind). Registered `ch2b_correlation_gross` KILLED (N_TRIALS 27). ❌ **CH2c whipsaw-aware governor KILLED** — de-risk only when stressed AND trends choppy (so trending crashes aren't cut). Hypothesis vindicated (beats the plain VIX governor +0.003, pulls BEAR −0.883→−0.791) but only +0.0006 vs constant-gross (n.s.) + still regresses BEAR → ship nothing (`ch2c_whipsaw_governor` KILLED, N_TRIALS 28). **⚠️ CH3 flag (scoped, NOT a verdict):** measured ONLY on the trend book's Sharpe + bear-regime Sharpe, the plain VIX crash governor (LIVE, default ON) doesn't beat static + worsens the bear tail (−0.883) — but this harness does NOT measure its actual mandate (portfolio drawdown/tail-insurance), which mechanically lowers Sharpe. A CH3 question on the right objective, NOT "turn it off"; nothing changed live here. ❌ **CH2a trend-strength/breadth gross KILLED** — lever up broad+clean trends / cut whipsaw, tested TWO ways (clarity + breadth); all 4 configs decisively worse (−0.07 to −0.19), premise refuted (trend strength is contrarian to forward Sharpe). **★ CH2 COMPLETE — all 3 sizing multipliers KILLED: conditional gross sizing does NOT beat constant-gross → keep the trend book SIMPLE** (a positive result; the constant-gross sizing is already near-optimal). No live change from any of CH2 (nothing passed its gate). ✅ **CH3 DONE (2026-07-08):** regime-conditional diagnostic (`scripts/ch3_regime_diagnostic.py`). **Q2 — NEITHER parked strategy is a conditional diversifier (both CLOSED):** `sector_rotation` decorrelates in BEAR (corr 0.40→0.14) BUT **loses −34%/yr there (Sharpe −1.26)** — a different way to lose, not a hedge; `credit_timing` collinear (0.33) + loses → CLOSED. **No CH4 candidate from the parked strategies.** (The first draft nominated sector_rotation on corr alone; the Opus review caught that decorrelation is mechanically confounded for long-flat strategies → added a standalone-value check that reversed it.) **Q3 resolves the CH2c flag — the plain VIX crash governor HELPS on its drawdown mandate** (full maxDD −1.8pp shallower, contiguous crises COVID −4.25% / BEAR_2022 −1.3% / GFC −0.4%, at −0.003 Sharpe) → **KEEP it** (the CH2c Sharpe drag is the expected insurance premium). ✅ **CH4 DONE (2026-07-08) — KILL + MORATORIUM:** the ONE pre-registered terminating search (ranging-MR sleeve, `app/research/ch4_mr_sleeve.py`) was KILLED — the primary fails 3/5 gates (Track-A CPCV 0.012, off-regime −0.11, Track-B t −0.52); the ranging regime is rare (2% active). The pre-registration caught a mirage: the one good-looking config (`mr_loose_regime`, CPCV 0.82, Track-B t 2.77) FAILED the detection-lag gate (0.82→0.16 under a 2-day regime lag) and — decision being on the primary only — couldn't flip the verdict. Registered `ch4_ranging_mr` KILLED (N_TRIALS 30). **★ The 12-month hunting MORATORIUM is now IN EFFECT (to 2027-07-08).** **COMPOUND-AND-HARDEN COMPLETE through CH4** — verdict: edge = trend+cash, conditional sizing doesn't improve it, parked strategies don't diversify it, the one search found nothing → operate it simply. **NOW: CH5** (live-forward accrual; pre-committed review 2027-07-08). No live change in CH0–CH4.**

**SSOT (IBKR migration): [`docs/reference/R1_ALL_IBKR_MIGRATION_PLAN_2026-07-06.md`](../reference/R1_ALL_IBKR_MIGRATION_PLAN_2026-07-06.md).** Decision (2026-07-06, owner): consolidate the ENTIRE live book (ETFs + cash + futures) onto **IBKR as the single venue** — migrate once, in paper, shadow-first. *(Futures leg now shelved per the review above; ETF/cash migration R1.1/R1.2 proceeds.)*

**Live book UNCHANGED** = Alpaca trend (paper) + cash (SGOV) + crypto paper. Every IBKR piece below is inert/shadow — nothing has placed a single IBKR order.

- **✅ Enforce flip is LIVE + VERIFIED (2026-07-06 flip; first rebalance verified 2026-07-13):** `pm.reconciliation_mode` + `pm.whole_book_gate_mode` = **enforce** (kill_switch_sm still shadow; drawdown ladder off). **✅ FIRST enforce rebalance (Mon 2026-07-13 09:45 trend / 09:50 cash) went CLEAN:** both enforce gates EVALUATED (not silently wedged) and passed — `[reconcile:trend mode=enforce] OK` + `[whole-book-gate:trend mode=enforce] OK` (gross 49.2%, net-equity-beta 0.42×, book 0.49× — within caps); `status=ok mode=live`, placed **6 trend + 1 cash** orders, 0 blocked, no breach/FAIL_CLOSED/gate_error. Book now = SPY/QQQ/IWM/EFA/EEM/UUP + SGOV (NAV ~$100.3k). **No spurious hold; the enforce flip works.** — **⚠️→✅ RESTART DONE (2026-07-14):** the 07-13 rebalance ran on STALE pre-07-07 code (daemon hadn't been restarted since the enforce flip), so CH0b's ungoverned-counterfactual + per-governor-mult capture was skipped for 07-13 (BENIGN — `overlay_mult=1.0`, no governor de-risk fired, so the counterfactual equalled the intended book). Daemon **restarted 2026-07-14 07:37** (graceful shutdown → startup reconciliation clean → `Scheduled enforce_rebalance_verify daily at 11:07 ET` confirms current code, i.e. CH0b/CH1/CH5 now LIVE; kill-switch off; dead-man watchdog fresh). **From 2026-07-20 onward it is fully automated:** the Monday rebalance captures the full CH0b scorecard + the 11:07 `enforce_rebalance_verify` job emails the PASS/ATTENTION summary itself (backstop `python -m scripts.verify_enforce_rebalance`). One-line revert if a future spurious hold: `set_agent_config(db,'pm.whole_book_gate_mode'/'pm.reconciliation_mode','shadow')`.
- **🔭 Shadow → enforce promotion queue (2026-07-21):** two governors remain in shadow (whole-book gate + reconciliation are already enforce). **(1) per-name gate (CH1):** ✅ soak tracker landed — each LIVE rebalance persists `per_name_metrics` on the scorecard row; `back_validation.per_name_soak_report()` gives the book-corr distribution + would-block counts at 0.80/0.85/0.90/0.95. 07-20 first clean current-code obs: book_corr **0.35 « 0.90** gate → **flip after ~3–4 more clean Mondays** (calibrate the threshold from the soak). **(2) kill-switch state machine (H2): ✅ ENFORCE — FLIPPED LIVE 2026-07-21.** `pm.kill_switch_sm_mode` = **enforce** (was shadow). The reduce-only refinement (`evaluate_rebalance()`) shipped first (#654), so an enforce `HALT_NEW_RISK` now runs the trend/cash rebalance **reduce-only** (trend: sells kept / buys suppressed; cash: buffer-replenish kept / deploy suppressed) — sheds risk without blocking protective exits. SM is currently `NORMAL`/dormant, so no behavior change until an auto-trigger (dead-man heartbeat / recon-FAIL_CLOSED) escalates it; the daemon (on the #654 commit) reads the mode live each rebalance (no restart needed). **Caveat: not yet exercised by a real drawdown** (calm-tape evidence). **One-line revert if it misbehaves:** `set_agent_config(db,'pm.kill_switch_sm_mode','shadow')`. Only the per-name gate (1) remains in shadow. Both flips permitted under the CH5 moratorium (hardening, not hunting).
- **✅ IBKR write+read surface built + merged (all inert, cutover-hardened; PRs #612–#627):** `IBKRConnectionManager` (dedicated-thread, fail-closed), `WritableIBKRAdapter` (shadow place/cancel; multiplier ONLY from instrument_master), front-month qualification + async fill capture, ETF→IBKR venue mapping (M1). All 16 futures live-qualified clean. Two Opus deep-dive hardening PRs folded in (connection shutdown-race, energy-FND-late, silent-delivery fail-safe — real bugs caught pre-merge).
- **✅ R1.1 shadow ETF routing ACTIVE (flag `ibkr.shadow_routing=on`):** trend+cash sleeves reconstruct each Alpaca order as the IBKR order they'd build, log a comparison, place nothing. **⚠️→✅ It had been SILENTLY SKIPPING** — the 07-14 daemon predated the `ib_insync` install, so both 07-20 rebalances logged `ib_insync unavailable — skipped` and accrued ZERO comparison data. The **07-21 restart** onto the venv (which has `ib_insync==0.9.86`) fixes it; a dry-run of Monday's real 8 orders reconstructs **8/8 clean, no mapping gaps** → real comparison lands 07-27. **✅ IBKR readiness probe added (2026-07-21):** `app/live_trading/ibkr_readiness.py` logs a one-line readiness verdict at daemon boot + `GET /api/ibkr/readiness` (ib_insync / shadow self-test / gateway reachability / account / Read-Only / blockers) so this class of silent gap can't recur. **Current stage = `R1.1_SHADOW_READY`** (only blocker: IB Gateway not running).
- **🛫 R1.2 tiny-live — IN PROGRESS (phased build).** **✅ Phase 1 DONE (2026-07-23): venue-routing seam.** `app/live_trading/execution_router.py` (`resolve_venue`/`get_execution_adapter`); both trend+cash sleeves now PLACE through the canonical `WritableBrokerAdapter` seam instead of `alpaca.place_market_order` directly, selected by `pm.{trend,cash}_venue` (**default `alpaca` = byte-identical**; `ibkr` → `WritableIBKRAdapter(mode='live')` on a `from_config` conn). Fail-safe: unknown/blank venue → alpaca; unknown venue in the factory RAISES. Zero live change (venue stays alpaca until Phase 3). Caught+fixed a latent test-fixture bug (fake used `sym/qty` vs the real `symbol/quantity`). +9 router tests; 149 sleeve/adapter/gate tests green. **⬜ Phase 2 (next): venue-aware reads** — positions / NAV / reconciliation read from the active venue. **⬜ Phase 3 (owner-present): the cutover** — start IB Gateway paper + set `ibkr.account` → turn Gateway **Read-Only OFF** (R1.0c-2b: live-place smoke + `whatIfOrder` margin) → flatten Alpaca → flip `pm.cash_venue`→ibkr (canary) → verify/reconcile → then trend. Nothing places an IBKR order until that explicit owner flip.
- **✅ R1.3 futures roll + signal DESIGNED & WIRED (inert/shadow):** roll policy (FND floor + last-trade cap), empirical roll-timing analysis → **HYBRID decision** (calendar for cash/FX, dynamic OI-crossover for physical — DECISIONS 2026-07-07), delivery-safety monitor (restart catch-up + CATASTROPHIC `futures_delivery_risk` alert), exchange holiday calendar (holiday-aware roll math), the **binding hybrid roll** (OI-crossover populated by `estimate_liquidity_roll`), and the **live carry+xSMOM signal wired → `futures_target_weights`** (`futures_signal.current_target_weights`, flag `ibkr.futures_signal_live` default OFF; still SHADOW). **Before live futures CAPITAL:** re-validate the edge on the hybrid roll (+ on the 16-market tradeable slice of the 76-market book), forward-target weight extraction (vs the 1-day PIT lag), fresh data feed.
- **🚨 R1.3 FUTURES-LIVE GATE FAILED (2026-07-07):** the carry+xSMOM book does NOT survive on the 16 IBKR markets — Track-B t (diversification vs live trend) **collapses 2.61 → −0.20** on 16 vs the full 76-market universe (standalone Sharpe 0.50 survives, but the diversifier rationale is gone). Methodology validated (full-76 reproduced GL-0's 2.61). **DECISION (DECISIONS 2026-07-07): don't deploy futures capital on 16 markets — EXPAND the IBKR futures universe (16 → toward 76, verify-on-connect) to restore breadth, then re-run `scripts/run_futures_16market_revalidation.py`; else shelve futures-live.** The built execution stack (roll/monitor/calendar/signal) stays valid + inert — the next futures work is a DATA/UNIVERSE task, not more plumbing.
- **Owner-gated remaining:** R1.0c-2b (Read-Only-API OFF live smoke — one real place + margin), R1.2 (tiny-live ETF cutover — UNAFFECTED by the futures gate).

---

## 🧭 PRIOR (2026-06-20): Alpha-v10 — 2nd external 5-LLM panel synthesized → plan set

**SSOT: [`docs/reference/ALPHA_V10_SYNTHESIS_AND_PLAN.md`](../reference/ALPHA_V10_SYNTHESIS_AND_PLAN.md)** (inputs + raw responses in `docs/reference/prompts/20260619_LLM_Alpha_V9/`). After equities+options+futures were all in hand, ran a 2nd brutally-honest 5-LLM panel (ChatGPT/Claude/DeepSeek/Gemini/Grok). **Unanimous verdict: we have RISK PREMIA, not alpha; excellent process; the book is a SINGLE BET (trend+cash); carry is promising but UNPROVEN (roll cost + no real fills); "the app is ahead of the alpha."** Consensus actions: (1) **carry's 0.66 is a phantom number until roll mechanics are modeled** — and beware DOUBLE-COUNTING (in commodities the roll yield IS the carry signal; charge transaction cost only); (2) kill in-sample vol-matching → PIT rolling vol everywhere (the +0.17 carry dSR + the +0.064 credit overlay likely shrink); (3) relabel trend/carry as ARP not alpha + size by drawdown not Kelly (freeze gross); (4) the highest-EV next moves are FREE (mine the futures factor zoo we own + CFTC CoT) and EXECUTABLE (IBKR futures plumbing) — not a data buy; (5) Norgate US Stocks ($693) is a "close-the-question" bounded equity audit, AFTER carry. **✅ Ran the panel's #1 diagnostic (the "trend contradiction"): live ETF-trend is post-2015 +0.77 (2020s +1.05) — NOT decayed → the live book is VINDICATED and the futures-trend kill confirmed.** **Plan = Alpha-v10:** P0 trust-the-numbers (carry honesty pass / ruler negative-controls / PIT-vol migration — free, gates all deploys) → P1 free futures factor zoo + CoT → P2 IBKR execution-truth build → P3 VRP-via-VIX-curve re-test → P4 bounded equity audit on Norgate stocks → P5 book resilience. ✅ **P0.2 carry honesty pass DONE 2026-06-20:** transaction-only roll cost (`app/research/futures_roll.py` + `roll_cost_bps`, 3bps/side baked into the official sleeve — does NOT subtract the roll yield → no double-count) + Track-B switched to budget-invariant residual-alpha. **Honest carry Sharpe 0.66→0.58** (still Track-A PAPER-PASS, point_SR 0.71, post-2015 0.81); **diversification REAL but MARGINAL** (residual-α t~1.8, resid-Sharpe 0.43 — the old in-sample +0.17 dSR was a vol-match artifact, ~0.00 under PIT; partly energy/VIX, ex-energy ~0.54). → carry stays a PAPER-candidate, measured deploy not urgent capital. 11 tests; 187 regression green. ✅ **P0.3 ruler negative-controls DONE 2026-06-20 → gate is CLEAN:** true-null PAPER FP rate 23.6% floor-alone → **5.3% JOINT with the HAC floor** (closes the leak to nominal); anti-correlated zero-edge null → Track-B residual-alpha pass-rate **5.7%** (~size, not gamed). `app/research/ruler_controls.py` + runner; 3 tests; Type-I controlled on both tiers → PASS/FAIL trustworthy. ✅ **P1.2 futures factor zoo DONE 2026-06-20:** reused `carry_backtest` as a generic XS engine; `app/research/futures_factors.py` (xs_momentum/curve_momentum/value/skewness) + runner. **Only cross-sectional 12-1 MOMENTUM survives → registered `futures_xsmom`: Sharpe 0.56, post-2015 0.58, corr-to-trend 0.12, official Track-A PAPER-PASS (point_SR 0.72).** curve-mom/value/skew KILLED at the pre-registered sign (no flipping = OPT-5 discipline). Now carry + XS-momentum = two real futures factors. 5 tests; 233 regression green. ✅ **P1.3 futures multi-factor book DONE 2026-06-20 → SIGNIFICANT diversifier (the second engine):** registered `futures_book` = equal-weight(carry, xsmom). **Book Sharpe 0.67 (post-2015 0.83), official Track-A PAPER-PASS (point_SR 0.85 — strongest sleeve yet); Track-B vs live ETF-trend residual-α t = 2.29 (SIGNIFICANT >1.96), resid-Sharpe 0.56** vs carry-alone t 1.76 — combining the two marginal factors crosses conventional significance ("gate the basket" works). 6 tests. The lead candidate to pair with live trend (still paper; needs P2 IBKR before capital). ✅ **P1.4 more free factors DONE 2026-06-20 → basis-momentum + CFTC CoT both KILLED; the free zoo is EXHAUSTED at carry + XS-momentum.** Basis-momentum (2nd-nearby built): Sharpe −0.10, residual-α t 0.47 — orthogonal but no edge. CFTC CoT hedging-pressure (Socrata API verified + 37 markets mapped, release-lagged PIT): full-sample Sharpe +0.06, **perfectly orthogonal (corr 0.03/0.01)** but residual-α t 0.27 → no edge (KILL; its 2010s→2020s improvement 0.14→0.77 noted as a regime-conditioned revisit candidate, NOT acted on; CFTC mapping preserved in ALPHA_V10 §P1). **Six free factors tested → only XS-momentum survived.** Lesson reaffirmed: orthogonality without standalone return doesn't help. **→ P1 done; the futures multi-factor book (carry + xsmom) is the product.** ✅ **P3.1 VRP-via-VIX-curve DONE 2026-06-20 → REVERSES the VRP park:** `app/research/vix_vrp.py` + `vix_vrp` sleeve — short the front VIX future (owned Norgate VX) in contango, flat in backwardation (crash-governor gated). **Sharpe 0.64 (post-2015 0.53, 2020s 0.51), HAC p 0.0018, Track-A PAPER-PASS (point_SR 0.47); SURVIVES the crashes (Feb-2018 −4.4%, COVID −4.8%) via the gate.** Marginal diversifier (corr-to-trend 0.46, residual-α t 1.46). Caveat: CPCV had no stress-regime fold → crash-survival is the manual event-study, re-confirm live. A 4th real risk-premium (trend, carry, xsmom, VIX-VRP). 3 tests. **NOTE: P4 (equity audit) is BLOCKED on the Norgate US Stocks ($693) purchase — separate from IBKR.** ✅ **IBKR account applied 2026-06-21** (IBKR Pro, all permissions, margin; approval ~Mon) → ✅ **P2 turnkey design spec written:** [`docs/reference/P2_IBKR_EXECUTION_DESIGN.md`](../reference/P2_IBKR_EXECUTION_DESIGN.md) (contract master + verify-on-connect, idempotency/broker-as-truth/snapshots/kill-switch, FastAPI decouple, replay parity, P2.1→P2.4 checklist). Adapter CODE waits for the live paper account (verify against the real API — building blind risks rework). Next: **on IBKR approval → P2.2-2.4**; meanwhile optional free cleanups **P0.4 credit-overlay PIT re-verdict + P0.5 family-level trial counting**; or buy Norgate stocks for P4. Live book UNCHANGED (trend 50% + cash; crypto paper). ✅ **DATA-QUALITY AUDIT DONE 2026-06-21:** read-only sweep (`scripts/audit_data_quality.py`) over every saved dataset → **the data behind every LIVE position + recent keep/kill verdict is CLEAN** (Norgate futures, macro VIX curve, FINRA, liquid ETF/large-cap prices). All defects are OFF-strategy: fixed 2 FMP fundamentals bugs (negative revenue → NaN; non-deterministic PIT period_end pick → deterministic sort, **zero data loss** 42009→42009) in `load_fmp_fundamentals` + 14 tests; documented equity-cache reverse-split jumps (GPOR/CHRD; swing-ML=off). **Discovered (follow-up, parked sleeve):** `factor_scorer.py` PIT date-col is misnamed → silent look-ahead (no live impact). Full report `docs/reference/DATA_QUALITY_AUDIT_2026-06-21.md`; DECISIONS 2026-06-21. 📤 **TWO external LLM review packs OUT 2026-06-21** (while IBKR pends): (1) **Go-Live Review** `docs/reference/prompts/20260621_Go_live_review/` — is the t=2.29 second engine real after family-wise multiple testing (Theme B), capital sizing/cross-venue risk architecture (A), and do the 4 premia co-crash (C; post-2015 pairwise corr avg ~0.49, higher than headline) — the first set already submitted, responses pending. (2) **Comprehensive Portfolio-Brain** `docs/reference/prompts/20260621_Comprehensive_portfolio_brain/` — design/re-architect the silo'd sleeves into one cohesive portfolio brain (consolidated book state, joint correlation-aware sizing, netting, cross-venue Alpaca+IBKR, single kill-switch, future-proofing) + where (if anywhere) LLMs belong in the live loop. Both synthesize 5-LLM panels on return. **Key as-built fact surfaced for the brain pack:** the LIVE sleeves (trend/cash) BYPASS the RiskManager (direct Alpaca + lightweight gate) — the RM's correlation/beta/heat checks only cover the OFF proposal-driven ML path, so there is no holistic risk gate on what actually trades. ✅ **GO-LIVE PANEL SYNTHESIZED 2026-06-21** (5/5 read, SSOT `docs/reference/GOLIVE_REVIEW_SYNTHESIS_2026-06-21.md`): **unanimous DON'T-deploy-capital-yet — the missing layer is risk governance, not alpha; 3 gates, all buildable in weeks, mostly free.** (B) **t=2.29 is UNPROVEN until a selection-aware null-strategy zoo runs** (uncorrected for ~20 families; carry likely survives, xsmom is a max-of-6 statistic [family p≈0.29], basket likely lands high-1s/low-2s → build the **carry-only fallback**); "gate the basket" is honest math but its evidentiary value hinges on pre-registration + xsmom's own null. (C) **the 4 premia are NET SHORT-CRISIS / "effectively one bet"** (unconditional corr misleading; test via exceedance corr + book down-β-vs-up-β + crisis replay) → **DROP VRP from the initial live book** (highest corr-to-trend 0.46, short-crisis, fitted gate); add a **defensive bond/gold/FX TREND sleeve** for convexity (NOT long-VIX options). (A) **ERC sizing (NO HRP, NO Kelly), ~8% book vol / −15-20% DD budget, margin ≤25% NAV, fractional paper-ramp**, drawdown + absorption-ratio de-gross ladders, a **cross-venue risk surface + state-machine kill-switch (HARD no-go gate before any IBKR capital)**, promotion ladder scaled by PROCESS fidelity not live t-stat. **Plan GL-0 null zoo (FIRST, free, = the deferred P0.5) → GL-1 tail test (parallel) → GL-2 risk surface (= Portfolio-Brain measurement layer) → GL-3 codify sizing/ladder → GL-4 drop/reformulate VRP + defensive sleeve → then IBKR paper→tiny-live→scale.** Also bundle a look-ahead audit of the carry/xsmom pipeline (PIT vol, expiry, stitch) — newest code, never paranoia-audited. Live book UNCHANGED. ✅ **PORTFOLIO-BRAIN ARCHITECTURE ROADMAP SYNTHESIZED 2026-06-21** (5/5 panel read + fused with Go-Live + adversarially self-reviewed: draft → 3 critics → v2 → verification gate → **v2.1, adoption-ready**; SSOT `docs/reference/PORTFOLIO_BRAIN_ROADMAP_2026-06-21.md`). **Unanimous design:** replace PM/RM/Trader with a deterministic weekly atomic **Target-Portfolio engine** (strategies = pure functions emitting desired exposures → one Constructor → risk-gate-the-target → execution-planner diffs the minimal netted order set → broker adapters); decouple daemon from FastAPI; Postgres=truth/Redis=ephemeral; **ERC/inverse-vol + dumb monotonic de-gross governors, NO Markowitz/HRP/Kelly**; broker abstraction + single cross-venue kill-switch; LLMs around-not-inside. **Critics forced a major re-scope (the key output):** stage into **R0 Minimum-Viable-Safety (shippable+TERMINAL: decouple → consolidated cross-venue book-state+reconciliation+kill-switch → route existing sleeves through a whole-book risk gate — captures ~80% of safety, the hard IBKR no-go gate) → 🚦 GL-0 ABORT GATE (null zoo fails/carry-only → STOP at R0, don't build the Constructor) → R1 new IBKR trading (carry tiny-live behind the gate, all hardening live BEFORE capital) → R2 unified Constructor (demand-pulled; covariance/ERC stack DATA-GATED behind ≥2 strategies × ≥6mo joint live returns — can't shrink a covariance never observed; inverse-vol terminal till then).** Don't build the coordination layer before there's something to coordinate. Risk-correctness fixes: tail-corr governor measures stress-conditional (not 63d avg) + GL-1 measured matrix wired into sizing; Go-Live verdicts = enforced fail-closed artifacts; per-venue IBKR margin reserve (Alpaca cash ≠ IBKR BP); controls-before-capital. **First 3 builds = decouple daemon, consolidated book-state in shadow, route sleeves through the gate.** Live book UNCHANGED. ✅ **GL-0 + GL-1 + R0.1 DONE 2026-06-21 (autonomous; report-only, no live-code change; SSOT `docs/reference/GL0_GL1_FINDINGS_2026-06-21.md`).** **GL-0 null-strategy zoo → BASKET_REAL:** the futures book is REAL not residue — empirical selection-aware max-stat null p 0.002 (book), carry p 0.002, **xs-momentum max-of-6 p 0.005** (clears the best-of-6 selection bar; 2.22 vs 95th-pct null 0.94); both carry+xsmom survive → no carry-only fallback; **DSR(N=20)=0.84 borderline → size MODESTLY**; pipeline **PIT-clean** (look-ahead audit, past byte-invariant to future). Reverses the pessimistic panel predictions; confirms "genuine but modest." (`app/research/null_zoo.py`, 11 tests, 6 deep-dive fixes incl. NaN-present-set BLOCKER + DSR-on-residual.) **GL-1 tail/co-crash → diversified + DROP VRP:** SPY-worst-5% stress corr **0.30 full / 0.40 post-2015** (both < 0.60 → NOT one-bet; trend is the crisis hedge offsetting convergent carry/xsmom: GFC trend +7% vs carry −14%/xsmom −5%, COVID carry −21%/xsmom −13%); **VRP most tail-concentrating (down/up asym +0.20, loses 6/8 crises) → drop from initial book / pair with long-vol**; defensive bond/gold/FX-trend sleeve borderline-recommended (core asym +0.08 [CI +0.04,+0.13]). (`app/research/tail_diagnostics.py`, 5 tests, 4 deep-dive fixes.) **R0.1 risk-policy v1:** frozen conservative artifact for the future whole-book gate (vol ~6-8%/−20% kill line, DD ladder, per-venue IBKR margin reserve, absolute notional caps, paper-ramp). (`app/live_trading/risk_policy.py`, 5 tests.) **→ R1 strategy set = trend + carry + xsmom (VRP DROPPED), sized modestly.** ✅ **R0.3 + R0.4 DONE 2026-06-21 (autonomous; SHADOW / read-only, controls nothing; SSOT `docs/reference/R0_FOUNDATION_2026-06-21.md`).** The R0 measurement+safety substrate: **R0.3** canonical instrument master (`instrument_master.py`; live Alpaca ETFs+T-bills verified, IBKR futures placeholders flagged verify-on-connect) + `BrokerAdapter` Protocol + read-only `AlpacaReadOnlyAdapter` (`broker_adapter.py`, structurally can't trade — no order methods + read-only client proxy). **R0.4** consolidated cross-venue `BookState` + netted factor-exposure view (`book_state.py`, full-notional so IBKR futures-MV trap avoided), reconciliation-before-trade keyed by **(venue, instrument_id)** FAIL-CLOSED on any material break (`reconciliation.py`), cross-venue kill-switch state machine with auto-triggers **capped at CANCEL_ONLY** (no auto-liquidate) (`kill_switch_state.py`). 23 tests; flake8 clean; deep-dive fixed the loss-vectors-when-wired (reconciliation cross-venue keying BLOCKER, futures full-notional BLOCKER, kill auto-cap, read-only client proxy). **Live book UNCHANGED** (modules inert until R0.5 wires them to the order path). **Deferred (owner-present/IBKR): R0.2 daemon decouple, R0.5 wire reconciliation+kill-switch+risk-policy to GATE the live path, R1 (IBKR adapter + verify-on-connect + carry/xsmom tiny-live), R2 (Constructor).** ✅ **R0.4b + R0.5 (SHADOW) DONE 2026-06-21 (with Min; owner-approved shadow-first; SSOT `docs/reference/R0_FOUNDATION_2026-06-21.md` §R0.5).** **R0.4b:** consolidated risk-surface report (`scripts/run_book_state_report.py`) validated on the LIVE Alpaca account (NAV $101,409; gross 20.3% of NAV; net equity-beta 0.16× NAV; all 5 trend ETFs mapped). **R0.5 whole-book risk gate (`app/live_trading/whole_book_gate.py`) WIRED into `trend_sleeve.run_trend_rebalance` in SHADOW** — computes the proposed-book risk-policy-v1 caps (gross-ex-cash, net equity beta, single/book notional, unmapped→fail-closed) and **LOGS + emails (`whole_book_gate_breach`) what it WOULD block, but blocks nothing** (flag `pm.whole_book_gate_mode` default `shadow`; `enforce` HOLDS on a breach fail-closed; `off` skips). **Deep-dive-verified provably inert in shadow** (double-wrapped fail-safe, no input mutation, no API call — a broken gate can't disrupt the live rebalance); enforce-hardening already applied (missing-price → fail-closed; enforce-mode wiring failure fails CLOSED). This is the **first holistic risk visibility on the actual live order path** (closing the "live sleeves bypass the RiskManager" gap). 9 gate tests; 594 trend/gate/orchestrator green; full suite green. **Live trading behavior UNCHANGED** (gate logs only). **Rollout: run shadow ≥1 week incl. Mon 2026-06-22 trend rebalance + first cash deploy → then flip `pm.whole_book_gate_mode`→`enforce` (config set, no code change).** Still deferred (owner-present): R0.2 daemon decouple; wiring reconciliation+kill-switch (not just the gate) into the live path; R1 IBKR. 📝 **R0.2 decouple ADR drafted 2026-06-21 (PROPOSED, owner-gated):** [`docs/reference/ADR_R0_2_DAEMON_DECOUPLE_2026-06-21.md`](../reference/ADR_R0_2_DAEMON_DECOUPLE_2026-06-21.md) — boundary inventory found the codebase already highly modular (singleton orchestrator/agents, kill-switch+capital-ramp already Postgres-persisted, inter-agent msging already Redis, "run both in one process" fallback = zero changes). **Phased default-off plan:** P1 capability (`MRTRADER_DAEMON_MODE`, default `in_process` = byte-identical to today; standalone `mtrader-tradingd` entry point) → P2 IPC bridge (web→agent control routes via Postgres/Redis) → P3 observability bridge (WS push + news watchlist + Trader cache) → P4 flip to `subprocess` + dead-man watchdog + systemd. **Recommendation: execute Phase 1 in a dedicated operator-present restart, NOT bundled with the shadow-gate restart.** Awaiting Min's approval of execution timing. ✅ **R0.2 PHASE 1 DONE 2026-06-21 (with Min; default-off / INERT — merging changes nothing):** `MRTRADER_DAEMON_MODE` (default `in_process`) + `app/trading_runtime.py` (brain lifecycle/preamble/shutdown-hardening, **no FastAPI import**) + `app/tradingd.py` standalone daemon (`python -m app.tradingd`). `main.py` lifespan boots the brain only when `web_boots_brain()` (mode ≠ subprocess); shutdown helpers re-exported under original names (test_shutdown_hardening unchanged). **Mutual-exclusion interlock:** web boots unless subprocess, daemon refuses unless subprocess → exactly one process runs the brain (no double-trading). Default mode byte-identical to pre-R0.2 → **safe to merge, no restart needed**; the behavior-changing flip to `subprocess` is Phase 4 (operator-present, after the P2/P3 control+observability bridges). 10 tests; full suite 3778 green; flake8 clean. **Live book UNCHANGED.** Next R0.2 steps (P2 IPC bridge → P3 observability bridge → P4 flip) remain owner-gated. ✅ **R0.2 PHASE 2 DONE 2026-06-22 (with Min; default-off / mode-conditional — in_process byte-identical):** `app/control_bridge.py` (command bus over Redis `pm_commands` + daemon consumer + 3 s Postgres kill-switch state-sync; **no FastAPI import**). Control routes (pause/resume, triggers cycle/swing/retrain/intraday, job pause/resume, capital-advance) are mode-conditional via `bridge_or_none()` — in_process run their direct path UNCHANGED, subprocess EMIT a command and never touch the web's empty orchestrator. Kill-switch: web closes positions + persists; daemon halts via state-sync poll + immediate `reload_state` command (agents read `kill_switch.is_active` live). Lazy per-route imports (a bridge-import error can't stop app boot); long triggers run as background tasks (never block pause/kill); consumer survives bad commands + cancels in-flight triggers on shutdown. **Verified to Min's extra-rigor bar: 3 deep-dive passes (1 self + 2 independent fresh-context reviewers), no BLOCKER, in_process byte-identical confirmed, fixes applied + re-verified; full suite 3801 green (twice), flake8 clean.** 23 tests. **Live book UNCHANGED.** Known subprocess-only limits documented (bounded kill window; capital-ramp in-memory → full bridge in R1; PM-trigger duplication). Next: P3 observability bridge (WS push + news watchlist + Trader cache) → P4 subprocess flip (operator-present) — owner-gated. ✅ **R0.2 PHASE 3 DONE 2026-06-22 (with Min; default-off / in_process byte-identical):** `app/observability_bridge.py` (daemon→web WebSocket relay over Redis pub/sub; **no FastAPI import**). **Finding:** only the WS push is a real cross-process gap — the news watchlist (PM-mutated, news_monitor-read, both in the brain) and the Trader mid-run cache (rebuilt from `trades` on start + reconciler) are brain-internal → no bridge needed (news-exits already fire in the daemon). Fix: daemon `publish_ws_event` → Redis channel `ws_events` (gated on `is_daemon_process()` via `tradingd.mark_as_daemon()`); web `ws_relay_loop` (started in `main.py` only when subprocess) re-broadcasts to the local WS manager. Pub/sub = ephemeral (no backlog); strict `json.dumps` mirrors `send_json`. in_process byte-identical (publish no-ops unless daemon; relay never starts; no double-delivery). **2 deep-dive passes (1 self + 1 independent), no BLOCKER/MAJOR; full suite 3816 green; flake8 clean.** 12 tests. **Live book UNCHANGED.** Known subprocess-only display gap: orchestrator-status panel reads the web's empty orchestrator (data is in Postgres; deferred). **All R0.2 bridge phases (P1-P3) now done; only P4 (the subprocess flip, operator-present) remains — owner-gated, gated on IBKR.** Also merged today: macro-banner staleness UI fix (#545). ✅ **P0.4 credit-overlay PIT re-verdict DONE 2026-06-22 (with Min; report-only, no live-path change) → PIT-ROBUST (panel "shrinks under PIT vol" hypothesis REFUTED).** The credit overlay is a scale-invariant time-varying de-risk multiplier on an already-PIT trend book → its dSharpe never depended on vol-matching (structurally unlike carry, whose +0.17 in-sample dSR collapsed to ~0 in P0.2). Re-ran `run_credit_curve` on current data: marginal-vs-VIX-governor **dSharpe +0.0639** (≈ +0.064), **dCalmar +0.0298**, all-3-crises (GFC +0.019/COVID +0.022/BEAR +0.006), both-halves dMaxDD +ve. **Did NOT shrink.** Status UNCHANGED — marginal tail-insurance **CANDIDATE, flag OFF**; binding caveat = multiplicity (post-hoc L=120/band=0.02), not vol. `curve_governor` unchanged (NO-BENEFIT, stays off). `scripts/run_credit_pit_reverdict.py` + 5 tests; independent review upheld the no-vol-artifact claim; full suite 3821 green. SSOT `docs/reference/P0_4_CREDIT_PIT_REVERDICT_2026-06-22.md`. (P0.5 family-level trial counting still available as a free cleanup.) 🐛 **BUGFIX 2026-06-22 (with Min) — trend/cash sleeve isolation:** the PM's 30-min swing-ML review was re-scoring trend/cash sleeve positions (filter excluded only intraday) and issuing EXIT/EXTEND_TARGET, fighting the weekly rebalancers — it had already force-closed a trend QQQ (Trade #132, `pm_nis_exit`). Fixed at source (`_is_swing_reviewable` excludes trend/cash) + sink (trader `_is_rebalancer_managed` guards EXIT/EXTEND_TARGET + reeval) + silenced the `audit_active_target_stops` false-corruption WARN on long-held trend winners. Independent review: no new leak, swing/PEAD/intraday preserved. 6 tests; full suite 3827 green. **Takes effect on next uvicorn restart** (low urgency). Tracked follow-ups: reconciler synthetic-adopt orphan-trend→swing race (narrow) + add `selector` to active_positions for dual-key symmetry. DECISIONS 2026-06-22 "trend/cash sleeve isolation". ✅ **BUGFIX FOLLOW-UPS DONE 2026-06-22 (with Min) — both closed:** **MAJOR** (orphan race) — new `startup_reconciler.classify_sleeve(symbol)` (CASH_ETFS + `pm.trend_universe`, exception-proof) replaces the hardcoded `trade_type="swing"` at BOTH synthetic-adopt sites (reconciler placeholder + trader fallback); an orphaned trend/cash leg (crash between fill and DB commit) is now classified to its real sleeve with vestigial stop/target left None → rebalancer-managed, not swing-liquidated. **MINOR** — `selector` added to all `active_positions` builders + the reeval message (dual-key `_is_rebalancer_managed` now genuinely live). **A FULL independent bug-check of the whole change-set (both bugfix PRs) returned SAFE TO DEPLOY — no BLOCKER/MAJOR.** 12 tests; full suite 3833 green; flake8 clean. trend/cash isolation now fully closed across both adoption paths, both review sinks, and the audit. **All these behavior changes take effect on the next uvicorn restart.** DECISIONS 2026-06-22 "orphan-position sleeve classification". ✅ **P0.5 family-level trial counting DONE 2026-06-22 (with Min; report-only) → P0 (trust-the-numbers) COMPLETE.** Replaced the hardcoded "~20 families" deflation estimate with an AUDITABLE registry: `app/research/family_registry.py` enumerates **25 distinct strategy families searched** (27 entries; cash + futures-book ensemble explicitly excluded) — LIVE 3 / PAPER 5 / KILLED 14 / PARKED 3 / SCAFFOLD 2, each with status + verdict + doc ref + a research degrees-of-freedom log. `family_trial_count()`=25 now feeds `null_zoo`'s parametric DSR (`dsr_family`/`n_families`; sensitivity band kept). The enumerated 25 > 20 → deflation slightly MORE conservative (monotone in N, verified). **GL-0 verdict UNCHANGED** (decided by the empirical max-stat p-values, not the DSR cross-check → 20→25 can't flip it; DSR stays borderline → confirms "size modestly"). Independent review verified both integrity properties (verdict independent of DSR; deflation monotone), no blocker. `scripts/run_family_registry.py` + 11 tests; full suite 3843 green. SSOT `docs/reference/P0_5_FAMILY_REGISTRY_2026-06-22.md`. 📐 **HOLISTIC MULTI-STRAT state + scope documented 2026-06-22** ([`docs/reference/HOLISTIC_MULTISTRAT_STATE_AND_SCOPE_2026-06-22.md`](../reference/HOLISTIC_MULTISTRAT_STATE_AND_SCOPE_2026-06-22.md)): **Live** = holistic measurement (R0.4 netted book-state) + whole-book risk gate (R0.5 shadow→enforce) built; joint correlation-aware SIZING deferred to R2 (data-gated). **Research** = combine-into-one-book (`assemble_book`, DYNAMIC inverse-vol), Track-B marginal-contribution, GL-1 cross-strategy tail, P0.5 family-deflation all built + standard discipline; gaps = unified combined-book CPCV-as-a-unit + gate-in-backtest + covariance-aware sizing. **Scope:** Phase A unified combined-book WF harness (FREE/buildable now — A2 combined-book CPCV + A3 gate-in-backtest are the real new work) → Phase B ERC/covariance sizing (data-gated = R2) → Phase C research↔live replay parity (R1/R2). **Phase A is the next free research build for multi-strat eval whenever we want it.** ✅ **PHASE A DONE 2026-06-22 (with Min; report-only) — unified combined-book WF harness:** `app/research/multistrat_eval.py` (`run_multistrat_eval`) evaluates a SET of strategies AS ONE BOOK — assemble (build_book, PIT inverse-vol) → run the book RETURN SERIES through the SAME CPCV as a single sleeve → book-level meanSR/path-t/%pos/worst + **family-level deflated Sharpe** (P0.5); optional risk-policy-v1 drawdown de-gross ladder (PIT) re-run; per-sleeve standalone SR + avg weight + **leave-one-out Track-B**; GL-1 cross-strategy tail; + a fold-in **union** book for ragged history (common-window = the CPCV verdict). Adding a strategy is now a one-call holistic check. **2 deep-dive passes (1 self + 1 independent): look-ahead-free (governor+union strictly PIT, no double-shift into CPCV) + DSR-argument-correct; review fixes applied (kwargs guard, union cost parity, warmup trim, strict governor test).** `scripts/run_multistrat_eval.py` + 11 tests; full suite 3855 green; flake8 clean. **Phase B (ERC/covariance sizing) + Phase C (research↔live replay parity) remain data-gated (= R2/IBKR).** DECISIONS 2026-06-22 (Phase A). ✅ **OPTION A sector-ETF rotation DONE 2026-06-22 (with Min; report-only) → real but REDUNDANT → PARKED.** First real use of the Phase A multi-strat evaluator: built `app/research/etf_rotation.py` (cross-sectional 12-1 relative-strength rotation over 11 SPDR sector ETFs — PIT signal `prices.shift(skip)/prices.shift(lookback)-1`, top-K=4, inverse-vol weight with a **vol floor**, Antonacci dual-momentum cash filter, 21-day rebal, 5bps cost). **Standalone PAPER-PASS (CPCV mean Sharpe 0.864, path-t 6.67, HAC p 0.0006) but Track-B FAIL vs the live ETF-trend book (residual-α corr 0.51, dSR +0.072, P(ΔSR>0) 0.875) → PARKED (redundant to trend; "gate the marginal contribution, not the standalone" confirmed the predicted null).** Registered in `family_registry` (N_TRIALS 25→26; GL-0 verdict unchanged — monotone). **2 deep-dive passes (1 self + 1 independent): NO look-ahead (price-perturbation injection confirms past byte-invariant); MAJOR zero-vol→inf-weight footgun fixed with vol_floor.** `scripts/run_sector_rotation_eval.py` + 9 tests; full suite 3864 green; flake8 clean. **Live book UNCHANGED.** DECISIONS + ML_EXPERIMENT_LOG 2026-06-22 (Option A). ✅ **ALPHA-v10 10-LLM PANEL SYNTHESIZED 2026-06-22 → "HARDEN, DON'T HUNT" (the active plan).** "While we wait for IBKR, find an overlooked method / deepen swing / better-trade what we have / make the app stronger" → ran 4 repo-grounded Opus panelists + 1 adversarial red-team + 5 external reviews (ChatGPT/Claude/DeepSeek/Gemini/Grok, told to ATTACK the internal panel). **UNANIMOUS (10/10): stop hunting a 5th sleeve for 1–3 months — the binding constraint is capital + a real *tiny-live* track record + not blowing up.** Three-phase plan **H (Harden) → D (Deploy) → R (Research)**, SSOT [`docs/reference/prompts/20260622_LLM_Alpha_V10/COMPREHENSIVE_ROADMAP_2026-06-22.md`](../reference/prompts/20260622_LLM_Alpha_V10/COMPREHENSIVE_ROADMAP_2026-06-22.md) (+ internal panel, red-team, 5 responses archived alongside). **Phase H = make the already-built safety layer LOAD-BEARING before any IBKR dollar** (reconciliation-before-trade fail-closed; kill-switch wired; gate shadow→enforce; pre-trade dollar-notional + futures-multiplier verify; out-of-band broker-only flatten; external dead-man watchdog; per-order idempotency; broker-side limits). **Key new findings:** the convexity gap is half self-inflicted (live sleeve is long-flat on equity-heavy universe → recover via long-short futures, not a new family; positive-carry crash convexity = unicorn); the #1 futures killer = contract-multiplier error; **operator-capacity/behavioral risk is the dominant UNMODELED risk** (assume backtest 0.72 → live ~0.4–0.5); even N=26 is optimistic. **DON'T (most dangerous):** vol-to-8% pre-harden. → **IMMEDIATE NEXT = Phase H, starting H10 (cash-ETF mapping SSOT + allocation doc-drift fix) + H3 (pre-trade dollar-notional sanity) — the safe prerequisites for the gate→enforce flip.** Live book UNCHANGED. DECISIONS + MASTER_BACKLOG 2026-06-22 (Alpha-v10 panel). ✅ **PHASE H — H10 DONE 2026-06-22 (with Min; ZERO live-behavior change) → unblocks the H2 gate→enforce flip.** The whole-book gate (shadow) would have fail-closed the entire trend rebalance in ENFORCE if `pm.cash_universe` were ever set to one of 5 cash ETFs `instrument_master` didn't know (it knew 3 of `cash_sleeve.CASH_ETFS`'s 8). Fix: `instrument_master.CASH_EQUIVALENT_ETFS` (all 8) is now the SINGLE SOURCE OF TRUTH — `cash_sleeve.CASH_ETFS` imports it (can't drift); registry seeds all 8; `book_state._FACTOR_MAP` gets the 5 missing as `{}`; fixed the stale `trend_allocation_pct (0.25)`→(0.50) docstring. `CASH_ETFS` VALUE byte-identical → every live gross-cap consumer unchanged; the delta is confined to the shadow gate + report-only book-state. **Independent Opus deep-dive: SAFE TO MERGE, no BLOCKER/MAJOR (zero-live-change CONFIRMED, circular-import safe, regression genuinely guarded).** 6 H10 tests + review-driven robustness fix; full suite 3870 green; flake8 clean. DECISIONS 2026-06-22 (Phase H — H10). **Next H items (owner-present / IBKR-gated):** H1 reconciliation-before-trade wiring + H2 gate→enforce; H4/H5 out-of-band flatten + dead-man; H3-IBKR multiplier verify. ✅ **IBKR PAPER ACCOUNT APPROVED + P2.2 READ-ONLY ADAPTER DONE 2026-06-22 (with Min; zero live-trade change — read-only, no order path).** TWS paper wired (DUQ869409, $100k; API port 7497, Read-Only, localhost-only). `app/live_trading/ibkr_adapter.py` `IBKRReadOnlyAdapter` (mirrors the Alpaca read-only adapter; connects `readonly=True`) + **verify-on-connect** (`verify_contracts` vs the live `reqContractDetails`) — the panel's #1-futures-killer mitigation, **validated LIVE: ALL 16 futures VERIFIED, 0 critical** after the live API CAUGHT + we corrected real spec errors (ZC/ZS multiplier 50→5000; FX/VIX request symbols 6E→EUR / 6J→JPY / VX→VIX; core book ES/NQ/RTY/CL/NG/GC/SI/HG/ZN/ZB/ZF matched). `ibkr.*` config (default `ibkr.enabled=false` = inert); `ib_insync==0.9.86`; `scripts/run_ibkr_verify.py`. Read-only by 3 layers (no order method + TWS Read-Only API + readonly connect). **Independent Opus deep-dive SAFE TO MERGE, no BLOCKER/MAJOR; 3 hardening fixes applied (verify-all-expiries / account-scoped / fail-closed reads) + re-verified live.** 14 tests; full suite 3884 green; flake8 clean. **This is the substrate the futures half of Phase H plugs into (H1 reconciliation / H4 flatten / H5 dead-man); still NO order path / NO capital (R1).** Wiring-step follow-ups (documented): ib_insync event-loop/thread handling off-main-thread; gate auto-connect on `ibkr.enabled`; `_ib` attribute-fence before R1. DECISIONS 2026-06-22 (P2.2). ✅ **PHASE H — H1 reconciliation-before-trade WIRED (SHADOW) 2026-06-22 (with Min; ZERO live-behavior change) — the panel's #1 safety gate, now on the live order path.** The pure `reconcile()` ("broker is reality, DB is memory") existed but was wired to nothing → the live rebalance could trade off a stale DB (uncommitted fill / manual close / partial). Now: `reconciliation.shadow_reconcile_before_trade()` (fail-safe: never raises, FAIL_CLOSED on internal error) compares the DB's book (ACTIVE+PENDING_FILL Trade rows → signed qty) vs broker-actual positions, keyed (venue, instrument_id), wired into **both** `trend` and `cash` rebalances BEFORE the risk gate (also closes the cash-bypass gap). `pm.reconciliation_mode` default **shadow** (logs + emails `reconciliation_break`, blocks nothing); `enforce` HOLDS fail-closed; `off` skips. Mode read outside the wiring try (enforce wiring failure → fail-closed) + normalized (so "Enforce" can't silently degrade to shadow). **Independent Opus deep-dive: SAFE TO MERGE, no BLOCKER; shadow zero-change CONFIRMED.** 3 review fixes applied (PENDING_FILL inclusion to kill the just-placed-order false break; mode normalize; notifier dedup). 18 tests (incl. sleeve-level shadow-proceeds / enforce-holds); full suite 3898 green; flake8 clean. **Live trading UNCHANGED (shadow logs only).** Rollout: run shadow ≥1 week → review `reconciliation_break` telemetry → fix surfaced DB↔broker modeling gaps (pending_qty from broker open orders; cross-sleeve status leftovers; cash-side check; wire IBKR positions into `extra_actual`) → THEN flip `pm.reconciliation_mode`→enforce (owner-present). DECISIONS 2026-06-22 (Phase H — H1). ✅ **PHASE H — H4 + H5 EMERGENCY MACHINERY DONE 2026-06-22 (with Min; additive operator tooling; ZERO live-trade-loop change).** **H4 out-of-band flatten:** `app/live_trading/emergency_flatten.py` + `scripts/emergency_flatten.py` — broker-only "close everything now" (cancel all orders + liquidate all via Alpaca `close_all_positions(cancel_orders=True)`), **DRY-RUN by default**, `--execute` to act, `--kill` to also trip the kill-switch; never raises; reports per-symbol 207 failures honestly (no false `ok`). **Validated LIVE in dry-run** (read the paper book — 7 positions incl. $49k SGOV — liquidated nothing). IBKR flatten = R1 (TWS Read-Only blocks orders). **H5 dead-man:** `app/live_trading/heartbeat.py` (atomic durable beat) written every 1 min by a new orchestrator job; `scripts/dead_man_watchdog.py` = EXTERNAL process that ALERTS (`dead_man_alert` email) on a stale/missing beat, **auto-flatten OPT-IN (default OFF)**, one-alert-per-episode + re-arm. **Independent Opus deep-dive: SAFE TO MERGE, no BLOCKER/MAJOR; accidental-liquidation safety CONFIRMED (liquidation only via explicit --execute or stale+--auto-flatten); the watchdog cannot miss a real death.** 3 review fixes (the key one: per-symbol multi-status → no misleading `ok`). 13 tests; full suite 3911 green; flake8 clean. **Live trading UNCHANGED** (only addition = a 1-min heartbeat file-write, inert; effective next restart). Operator: run `scripts/dead_man_watchdog.py` as a separate always-on process; test `scripts/emergency_flatten.py` weekly (dry-run safe). DECISIONS + MASTER_BACKLOG 2026-06-22 (Phase H — H4/H5). ✅ **PHASE H — H6 PER-ORDER IDEMPOTENCY DONE 2026-06-22 (with Min; normal placement UNCHANGED — only the dup-retry path improves).** Both sleeves already used deterministic client_order_ids (Alpaca dedups → no double-fill), but a crash-retry's duplicate-rejection was logged `order_error` + skipped (orphan). Now `alpaca.place_market_order` is **idempotent**: on a duplicate-client_order_id `APIError` it fetches the existing order (`get_order_by_client_id`) and returns it (`idempotent_reuse=True`, no circuit-breaker trip); conservative dup detector; dup-but-lookup-fails → raise + CB. Centralized the scheme in `app/live_trading/order_ids.py` (`idempotency_key`, byte-for-byte the old strings); trend+cash route through it + log reuses. **Independent Opus deep-dive: SAFE TO MERGE, no BLOCKER/MAJOR; cannot cause/mask a double-fill or missed trade** (one-key-per-intended-order invariant verified). 9 tests; full suite green; flake8 clean. Establishes the idempotency-key pattern P2.3 (IBKR order path) reuses; heavier per-order lifecycle log deferred to P2.3. DECISIONS + MASTER_BACKLOG 2026-06-22 (Phase H — H6). ✅ **PHASE H — H8 ALERTING DONE 2026-06-22 (with Min; alerting/observability only; ZERO live-trade change).** Notifier now tags each event with a **severity tier** (CRITICAL/WARN/INFO; subject prefixed for triage) + a `_route_critical` **louder channel** for CATASTROPHIC events — a generic webhook (`MRTRADER_CRITICAL_WEBHOOK`) fired fire-and-forget on a daemon thread (zero caller latency, never raises, **no-op until configured** → set the env var to a Pushover/Slack/SMS-relay URL to enable SMS/push). The whole-book gate now emits a **`gate_error` alert** (was a silent debug log) when it can't evaluate — while STILL returning fail-safe `allow=True` (gate semantics unchanged). **Independent Opus deep-dive: SAFE TO MERGE, no BLOCKER/MAJOR; no live-trade behavior change** (1 MINOR fixed: webhook now threaded, was a ≤3s blocking call). 10 tests; full suite 3930 green; flake8 clean. DECISIONS + MASTER_BACKLOG 2026-06-22 (Phase H — H8). ✅ **PHASE H — H9 DRAWDOWN LADDER DONE 2026-06-22 (with Min; SHADOW-default; ZERO live-behavior change while the flag is off).** `RISK_POLICY_V1`'s drawdown ladder (−8/−12/−16/−20% → ×0.75/0.50/0.25/0.00) is now wired into the live trend budget: `trend_sleeve._drawdown_ladder_multiplier` reads the account HWM `risk.peak_equity` (RiskManager-maintained, in `config_store`), computes drawdown-from-HWM, and de-grosses the trend `alloc` **UN-floored** (unlike the 0.25-floored VIX/credit governors) so the −20% kill rung (0.0) **flattens the held book** (alloc=0 → `compute_trend_deltas` emits full-exit SELLs, not merely halts buys). Flag `pm.drawdown_ladder_enabled` default **`false`** = SHADOW (computes + logs the would-be cut, applies 1.0); helper can only reduce (≤1.0), never raises, fail-safe to 1.0 (missing/zero HWM, NAV≤0, NAV≥HWM). Re-scales `intended_weights` + the `decision_audit` `size_mult` by the composite (floored-governor × ladder). **Independent Opus deep-dive: SAFE TO MERGE, no BLOCKER/MAJOR; default-off zero-live-change CONFIRMED; the kill rung provably SELLS to flat; helper can never increase exposure or raise.** 2 MINORs fixed (audit size_mult now records the composite; a sleeve-level test proves the −20% rung emits full-exit SELLs against a held SPY/QQQ book). 8 H9 tests; full suite green; flake8 clean. **Live trading UNCHANGED (shadow logs only).** Rollout: arm with `pm.drawdown_ladder_enabled=true` after the clean shadow week (owner-present). DECISIONS + MASTER_BACKLOG 2026-06-22 (Phase H — H9). ✅ **P2.3 IBKR FUTURES ORDER PATH (SHADOW) DONE 2026-06-22 (with Min; provably inert — places nothing).** The order-construction half of the IBKR futures path, shadow-first: `futures_sizing.py` (`target_lots` — multiplier sourced ONLY from `instrument_master`, no caller param = the #1-killer guard; + dust/per-market/asset-class caps; `futures_order_deltas` reductions-first/full-exit), `order_ids.futures_run_id`/`futures_order_ref` (deterministic run-id orderRef), and `futures_sleeve.run_futures_rebalance` (mirrors trend: kill-switch → verify-on-connect drops CRITICAL-mismatch instruments → IBKR-scoped reconcile-before-trade → whole-book gate `venue=IBKR` → **LOG would-be orders + shadow audit, PLACE NOTHING**). **Inert by FOUR layers:** `ibkr.enabled=false` (no connect) + `ibkr.futures_enabled=false` (dormant) + `ibkr.trading_mode=shadow` (`live` still places nothing in P2.3) + NO write API exists (compile-time guard); not scheduler-wired; consumes an EXPLICIT STUB weight source (`ibkr.futures_target_weights_json`) — NOT a live signal. **Independent Opus deep-dive: SAFE TO MERGE, no BLOCKER; inert-by-default + cannot-place-even-if-flipped CONFIRMED; no Alpaca contact; sizing/delta/run-id math + gate symbol-domain wiring correct.** One MAJOR fixed: the futures reconcile dragged the whole Alpaca DB book in as phantom breaks → added an `expected={}` override to `shadow_reconcile_before_trade` so it reconciles ONLY the IBKR slice. 31 tests; full suite green; flake8 clean. **Deferred to R1 (owner-gated: TWS Read-Only API OFF + owner-present + live-paper soak): real placement, fills capture, margin preview, roll emission, the live carry/xsmom signal path, the per-run snapshot table, scheduler wiring.** DECISIONS + MASTER_BACKLOG 2026-06-22 (P2.3); P2_IBKR_EXECUTION_DESIGN §6 updated. **Phase H remaining: H2 (kill-switch wiring + gate→enforce, owner-present after the shadow week) + H3-IBKR (order-time multiplier). The IBKR order PATH now exists in shadow; the live flip ("moving to IBKR") is R1 — after the shadow week + enforce + Read-Only-off.**

### 🛡️ Phase-H hardening progress (2026-06-25 → **ENFORCE FLIP LIVE 2026-07-06**)
Toward the IBKR no-go gate: **H1 enforce-prep DONE** (#599 — reconciliation now models in-flight orders → no just-placed-order false break; shadow soak clean since 06-22). **H3 DONE** (#600 — pre-trade per-order fat-finger backstop at the Alpaca chokepoint, $500k/100k-share fail-closed caps). **H2 code-half DONE** (kill-switch state machine wired shadow-first: `kill_switch_sm` + `evaluate_new_risk` at trend/cash/RM gates, recon/heartbeat auto-triggers, `pm.kill_switch_sm_mode` default shadow). Each Opus-deep-dived, tested, green. **✅ ENFORCE FLIP DONE 2026-07-06 (owner-present):** after the ~2-week clean shadow soak + the **first post-power-outage live rebalance reconciled clean** (07-06 09:45 trend + 09:50 cash: `[reconcile:trend/cash mode=shadow] OK`, `[whole-book-gate:trend mode=shadow] OK`, no breaks/breaches), flipped **`pm.reconciliation_mode` + `pm.whole_book_gate_mode` → `enforce`** (config-only, read live per-rebalance, instantly reversible; no restart). Now a DB↔broker break OR a risk-policy cap breach HOLDS the rebalance fail-closed ("a missed rebalance, never a bad trade" — Opus pre-flight GO, verified no liquidation/short/partial path). **`pm.kill_switch_sm_mode` stays `shadow`** pending the cash reduce-only refinement; `pm.drawdown_ladder_enabled` stays off. First enforce test = the next rebalance (07-07 09:45/09:50) — watch that the gate EVALUATES (not silently holds). DECISIONS 2026-07-06. Eval re-runs (audit 6a/6b): L/S WF 0/4 PASS (ranker dead, live long-only unaffected); intraday WF blocked (no model sentinel; dead+off) — see ML_EXPERIMENT_LOG.

### 🛰️ Side-track (2026-06-25): Macro Intel dashboard + macro-risk workflow — COMPLETE (F12 flags now ENABLED, paper)
Operator-driven fix of the Macro Intel tab + the NIS macro-risk workflow (scope SSOT `docs/reference/MACRO_INTEL_SCOPE_2026-06-25.md`). Shipped (#587–#594 + F12b): **Phase 0/1/2a** decision-linkage date-scope + skip-row collapse + ET timestamps + polarity-aware Beat/Miss (Cooler/Hotter/risk-on-off). **Phase 2b** macro risk now STEPS DOWN after benign releases (prompt rewrite + deterministic floor/clamp; gate provably never less conservative). **F10** SIZE-DOWN folds the macro factor into per-symbol display (honest "advisory until F12"). **F11** idempotent skip-audit (no duplicate rows on re-trigger). **F12a** `UNIFIED_MACRO_SIZING` (default OFF) — PM folds the graded NIS macro factor into per-symbol entry qty. **F12b** `MACRO_TIGHTEN_EXITS` — Trader tightens swing stops on a digested-adverse macro read (never liquidate). **✅ Both F12 flags ENABLED 2026-06-25 (paper, owner-approved)** — the live paper book now exercises macro entry-sizing + exit-tightening (effective on the uvicorn restart after the flip-PR merges; independently revertible via False + restart). Each phase shipped with an Opus adversarial deep-dive + tests; full suite green throughout. The two hardcoded macro gates (RM Rule 0 + `app/calendars/macro.py` ±window) are the untouched deterministic safety floor. **✅ Also fixed (#597, 2026-06-25):** recurring spurious `INTRADAY_FORCE_CLOSED AAPL` (phantom in-memory intraday position with no DB trade / no Alpaca position, force-closed every cycle when the Alpaca fetch failed) → new DB-authoritative `Trader._purge_phantom_intraday_positions()` (startup + failed-fetch fallback; never drops a real position; Opus-clean). Cleaned 304 unreal AAPL DB rows (83 phantom force-closes + 219 swing/skip collapse-artifacts + 2 residual), archived to `docs/archive/data/phantom_aapl_cleanup_2026-06-25.json`; confirmed **no real AAPL position/short ever opened**. Live since the 2026-06-25 14:30 ET restart (0 recurrences).

## 🧭 PRIOR (2026-06-16): Alpha-v9 — external 5-LLM review synthesized → roadmap + architecture documented

**Alpha-v8 is COMPLETE** (G0–G4 merged #495–#499: credit-selective overlay = CANDIDATE flag-OFF; G2 short-interest KILLED; G3 PARK). We assembled an external review pack (`docs/reference/prompts/20260616_Alpha_v8/`) and got **5 independent world-class-quant reviews** (ChatGPT, Claude Max, DeepSeek, Gemini, Grok). Opus synthesized them into the **Alpha-v9 program**:
- **SSOT roadmap:** [`docs/reference/ALPHA_V9_ROADMAP.md`](../reference/ALPHA_V9_ROADMAP.md) — critical synthesis (consensus, disagreements + my calls, the sharp critiques that change decisions).
- **SSOT design:** [`docs/reference/ALPHA_V9_ARCHITECTURE.md`](../reference/ALPHA_V9_ARCHITECTURE.md) — multi-engine-book architecture + phased implementation plan.

**Headline:** process is fund-grade; "free daily US **equity directional** alpha is mined out" is correct — but we mistook one quadrant for the whole map. **The prize:** (1) **validate the validator** (positive-control the feature→label pipeline — the "all early bugs inflated" claim is NOT safe; we had *deflationary* bugs too, e.g. #PERFOLD2 empty-X), (2) fix two gate flaws quietly killing diversifiers (unpowered both-halves guard ~24% FN @ SR-0.5; Track-B standalone-SR floor), (3) run a **second return engine — defined-risk VRP — paired with trend by skew** (positive-skew ⊕ negative-skew under the governor), (4) point trend at **crypto** (Alpaca-executable), (5) buy **Norgate (~$270/yr)** → un-bias event work + unlock **carry**. Quick wins: explicit **cash/T-bill sleeve** (~75% idle earns RFR), trend-allocation decision, credit-overlay live decision, live-fill back-validation.

**Phase 0 ✅ COMPLETE (2026-06-16).** ✅ **P0-1 DONE (2026-06-16) → PASS.** `scripts/walkforward/positive_control.py` pushes known anomalies through the REAL feature→label pipeline; FIDELITY-centered verdict (pipeline IC vs an independent reference IC) → 3/3 faithful, 0 deflationary, label-fidelity +0.761 (confirmed on 4y AND 12y). **The feature path is sound — IC≈0 is the market/model/cost, NOT a deflationary bug → the "mined out" conclusion holds on the feature side, and the equity-ML kills do not re-open on pipeline-bug grounds.** Finding for P5: swing features are window-limited to ~63 bars (`momentum_252d_ex1m` is really ~42-bar — the pipeline never sees true 12-month momentum). 2 Opus deep-dives, 12 tests. ✅ **P0-2 DONE (2026-06-16) → SUCCESS:** powered stability test (`app/research/stability.py`) replaces the binary both-halves guard (sim 22%→3.8% FN on a true SR-0.5 edge); Track-B standalone-SR floor report-only for diversifiers; probation `P(ΔSR>0)≥0.75` (live-paper-ratified). Re-running carry F3 (which the old guard killed) → **`PROMOTE_PAPER_PROBATION`** — the old guard was the problem, as predicted. 2 Opus deep-dives, 11 tests, report-only (promotion owner-gated). ✅ **P0-3 DONE (2026-06-16) → SUCCESS:** flipped `REQUIRE_TRUE_WF_FOR_PROMOTION` False→True — a TRAINED model can no longer promote on a frozen generalization test (must per-fold-retrain); `is_true_walkforward = per_fold OR rules_based` (rules-based sleeves OOS-by-construction, unaffected). Opus CRITICAL C1: a frozen run blocked by this alone → `INCONCLUSIVE` (report-only), NOT RETIRE, so `retrain_cron` can't roll back the champion. +5 tests; full suite 3544 pass / 0 fail; report-only (retrain currently disabled). **→ Phase 0 (validate-the-validator) COMPLETE. NEXT: Phase 1** (harden & monetize: cash sleeve P1-1, trend-allocation decision P1-2 👤, credit-overlay decision P1-3 👤, live-fill back-validation P1-4). Live book: trend + cash; VIX governor ON; credit overlay flag-OFF (shadowing).

**Phase 1 IN PROGRESS (started 2026-06-16).** ✅ **P1-2 (trend allocation) — DECIDED: 25% → 50%.** `scripts/analyze_trend_allocation.py` on the real 19.4y trend book (100%-gross: Sharpe 0.72, ann vol 9.34%, maxDD −13.9%): **Kelly is NOT the binding constraint** — full Kelly = 7.7× gross (774% NAV), and even a brutal Sharpe haircut keeps it >400%. The old **25% ran the sleeve at just 2.3% standalone vol** — far below any defensible sizing of the only live edge. Owner approved **50%** (the recommended pick; ~4.7% standalone vol / ~−7% maxDD — deliberate doubling, deeply Kelly-haircut, VIX-governor-protected, under the 80% cap). **`pm.trend_allocation_pct` flipped 0.25→0.50 in the live DB + schema default; takes effect at the next trend rebalance (no restart). Revisable as live-paper data accrues (P1-4).** See DECISIONS 2026-06-16 (P1-2). 🔭 **P1-3 (credit overlay) — SHADOW mode, monitoring.** Owner chose to shadow the G1 credit overlay a few weeks before enable/park/kill. Built `scripts/shadow_credit_governor.py` (report-only; reconstructs the credit + VIX multipliers from settled HYG/IEF closes in `macro_history.parquet` — touches NO live code) + `tests/test_shadow_credit_governor.py`. Validated on 2020→now: fires ~8% of days on real credit stress (COVID Feb-Mar 2020 etc.), marginal +2.87% cum / Sharpe 1.090→1.165 / maxDD −8.7%→−7.6% (the "small tail-insurance" effect). Shadow window opened 2026-06-16; **scheduled review ~2026-07-14** (cron) to verdict enable/park/kill. Live book UNCHANGED. ✅ **P1-4 (live-fill back-validation) — DONE 2026-06-16 → instrument SHIPPED (BUILDING until ~early July).** `app/live_trading/back_validation.py`: a date-matched **intended-vs-actual** tracking-error instrument for the trend sleeve — captures the sleeve's own intended weights at each rebalance (post inverse-vol+alloc+governor) and replays them on the SAME Alpaca prices + SAME calendar as the actual held book, isolating pure execution friction (rounding, gross/per-name caps, crowding, partial fills, timing). Reports corr / annualized TE / drift / execution-drag + a PASS/WATCH/FAIL/BUILDING verdict; daily EOD snapshot + weekly email wired into the orchestrator; CLI `scripts/back_validation_report.py`. **2 Opus deep-dives:** rejected an independently-reconstructed backtest sim as contaminated (different rebalance calendar + adjusted-vs-raw prices) → pivoted to intended-vs-actual; CRITICAL caught + fixed (shadow runs would record intent vs an empty book → spurious FAIL; now live-only). Also fixed a latent bug (`trend_weekly` emails were dropped — unregistered event). 17 tests; full suite 3564 pass; report-only. Trend went live ~2026-06-15 so the verdict reads BUILDING until ~15 trading days accrue. ✅ **P1-1 (cash/T-bill sleeve) — DONE 2026-06-16 → instrument SHIPPED (default OFF; owner-flip to enable).** `app/live_trading/cash_sleeve.py` + `cash_tracker.py`: parks idle SETTLED cash (beyond a `pm.cash_buffer_pct` buffer) into a T-bill ETF (SGOV/BIL) so the ~50% idle book earns the risk-free rate instead of zero; sells T-bills to refill the buffer when cash dips below it (risk-off). Weekly 09:50 ET after trend. **T-bills are cash-equivalents → EXCLUDED from the 80% risk gross cap + open-position/strategy-budget/sector counts** (risk_manager, portfolio_manager, trend_sleeve, risk_rules) so parking cash never starves trend/PEAD/swing; tagged selector/trade_type='cash' → Trader + reconciler leave them alone. **Opus deep-dive = DON'T-SHIP → fixed** (gross-exclusion was in only 2/6 gates; a Trader self-adoption path would liquidate the buffer; SPY-anchored price fetch made risk-off sells silently no-op; settlement race hardened). 14 tests; full suite 3578 pass. ✅ **ENABLED LIVE 2026-06-16** (`pm.cash_enabled='true'`+`pm.cash_shadow='false'` via new `scripts/set_cash_config.py --enable --arm`, +6 tests): a live shadow dry-run showed ~76% of NAV ($77k of $101k) idle → first live rebalance **Mon 2026-06-22 09:50 ET** deploys ~$75k into SGOV (≈$3.7k/yr RFR). See DECISIONS / MODEL_STATUS 2026-06-16 (P1-1 enable). **→ Phase 1 buildable work COMPLETE (P1-1/P1-2/P1-4). Only P1-3 (credit-overlay shadow verdict ~mid-July) remains, time-gated.**

**Phase 2 IN PROGRESS (started 2026-06-16).** ✅ **P2-4 (premium-% option cost model) — DONE → framework SHIPPED, calibration PRELIMINARY.** `app/options/spread_model.py::CalibratedSpreadModel` replaces the flat 1%-of-premium option half-spread with an EMPIRICAL surface calibrated from the live NBBO log: median relative spread bucketed by (underlying × moneyness × DTE × call/put), hierarchical fallback → conservative p75 global; `CalibratedOptionsSpreadCostModel` wired into the OPT-2 sim (flat 1% stays default, calibrated opt-in); `scripts/calibrate_option_spreads.py`. Calibrated on 209K obs → SPY ATM 30D ≈ 0.86% half-of-premium, deep-OTM ≈ 33% (correctly costs liquid index VRP cheap, single-name/OTM dear). **2 Opus deep-dives (SHIP)** fixed 3 CRITICAL: anachronism (~4-day 2026-06 snapshot → `covers_date()` guard + NOT wired to any live VRP verdict), too-cheap global fallback (→ conservative p75), and the IV-aware claim (it's moneyness/DTE-aware; premium-% gives IV-sensitivity). 16 tests; full suite 3603 pass. **Blocking prerequisite for the Phase-3 VRP engine cleared** — but a real VRP go/no-go needs a mature NBBO surface (spanning the test window) + live-paper validation + the per-trade covers_date guard wired in. Remaining P2: adapters (P2-5). Live book UNCHANGED.

**Phase 3 STARTED (2026-06-16).** ✅ **P3-1 (crypto trend) — DONE → criterion MET, PAPER-CANDIDATE (not capital).** Built `app/data/alpaca_crypto_provider.py` + a `crypto_trend` Sleeve (TSMOM on 10 Alpaca spot pairs, long-flat, ann=365 + calendar lookbacks, hard book_vol_target=0.20, 25bps). **Standalone Sharpe 0.64 (365-ann), corr-to-ETF-trend 0.18 → the falsifiable criterion (Sharpe>0 AND corr<0.6) is MET = a real, lowly-correlated stream.** Sleeve Lab: PAPER PASS (point_SR 0.96, hac_p 0.038); CAPITAL FAIL (power floor — ~5y history/no live-paper); **Track-B vs trend book FAIL (dSR −0.17 — doesn't improve trend-only)**. **→ PAPER-candidate: run live-paper to accrue the OOS record; revisit as a multi-sleeve-book diversifier (its low corr helps the whole book, not vs trend alone). Not a capital allocation.** 2 Opus deep-dives fixed C1 (gate now annualizes 365 via `periods_per_year`; equity unchanged at 252), M1 (partial-bar PIT), M3 (2× cost Sharpe 0.557). 7 tests; full suite 3611 pass; report-only. Live book UNCHANGED. ✅ **P3-1 LIVE-PAPER ENABLED 2026-06-16** (report-only, NO capital): `app/live_trading/crypto_paper_track.py` recomputes the rules-based crypto book on live Alpaca closes weekly and freezes the forward OOS slice (Sharpe-to-date vs 0.64 backtest); `pm.crypto_paper_enabled=true`, orchestrator-wired (09:55 ET, 24/7, no market gate), `crypto_weekly` email, CLI `scripts/crypto_paper_report.py`. **OOS clock started 2026-06-16.** ⚠️ scheduled job needs a uvicorn restart (runnable manually meanwhile). 9 tests. See DECISIONS 2026-06-16 (P3-1 live-paper). ✅ **P3-3 (overnight vs intraday) — DONE → ❌ KILL (clean).** `app/research/overnight_intraday.py` decomposes equity returns into overnight (close→open) vs intraday (open→close) legs + a net-of-cost cost-grid sweep (frozen `P3-3-OVERNIGHT-INTRADAY`). On the liquid-ETF universe (SPY/QQQ/IWM/EFA/EEM/DIA, 2007→/4891d) **the overnight premium is genuinely real (gross Sharpe +0.53 vs intraday +0.27) but cost-killed: net overnight Sharpe +0.53→+0.16 (1bps/side)→−0.22 (2bps); at realistic cost +0.16 < 0.30 floor → KILL.** Turnover is the whole risk, as predicted. Opus deep-dive (no bugs): legs reconcile to close-to-close at machine epsilon (adjustment-basis valid, no look-ahead). 11+3 tests; report-only. The overnight leg is retained as a candidate component for the P3-4 risk-premia composite. ✅ **P3-5 (FINRA daily short-volume) — DONE 2026-06-17 → REAL but real-but-weak (not a standalone edge).** Built `app/data/finra_short_volume.py` (daily CNMS files, 2019→/1,875 days = ~10× the killed-for-power G2 short-interest) + `app/research/short_volume.py` (aggregate-timing signal, frozen `P3-5-SHORTVOL-AGG`). Free-data test is AGGREGATE (XS would be survivorship-biased → post-Norgate). **Signal is genuinely real** (overlay net Sharpe 0.99 > buy-hold 0.92, HAC p 0.001; informed-short tercile high −0.009% < low +0.096%; opposite −0.34; ex-COVID robust) **but NOT standalone:** residual alpha vs SPY insignificant (+2.6%/yr, t 0.95 — mostly timed beta β0.83) and sub-period unstable (H1 −0.06 / H2 +0.23). **→ route to the P3-4 composite + cross-sectional post-Norgate.** Powered short-volume data layer now owned (G2 power problem solved). 14 tests; report-only.  **Phase 3 status: P3-1 paper-candidate (live-paper running), P3-3 KILL, P3-5 real-but-weak; P3-2 VRP gated on NBBO maturity (~mid-July); P3-4 composite now has 2 real components (overnight, short-vol) → buildable next.**

**Phase 4 STARTED (2026-06-17).** ✅ **Bought Norgate Futures "Silver" (~$297/yr) + mirrored locally.** Norgate is access-licensed (local NDU DB, not owned), so `app/data/norgate_provider.py` mirrors the futures data into our OWN parquet store `data/norgate_futures/` (gitignored — licensed): **105 markets, 23,575 individual contracts, ~12.1M rows, 174 MB** (continuous back-adj+unadj + full term structure; 30+yr — GC to 1978, CL to 1983). `load_continuous()`/`load_contracts()` read parquet only (no NDU dependency). 8 tests. **US Stocks Platinum (P4-1) DEFERRED on cost** ($693/yr; re-tests already-dead PEAD/F2). See DECISIONS 2026-06-17 (P4) + DATA_PROVIDERS (Norgate). ✅ **P4-2 (futures trend + carry) — DONE 2026-06-18 → CARRY is a real, MODERN, diversifying edge (first new deployable edge since trend).** `app/research/futures_data.py` (correct difference-adjusted returns `ΔCCB/unadj_prev`, winsorized; 73-market liquid/non-micro/non-STIR universe) + `futures_carry.py` (term-structure carry) + sleeves `futures_trend`/`futures_carry` + `scripts/run_futures_research.py`. **CARRY: Sharpe 0.67, positive in EVERY sub-period incl. modern (post-2015 +0.84, 2010-19 +1.00), cost-robust; Ruler-v2 Track-A PAPER-PASS; corr-to-live-trend 0.25; Track-B dSR +0.17 (0.72→0.89).** **TREND: real historically (0.83) but DECAYED — entirely pre-2010 (post-2015 +0.02) → not standalone; the crisis-convex partner (corr-to-carry 0.10).** **TREND+CARRY book Sharpe ~1.0, post-2015 +0.57, DD −29%.** 18 tests; report-only. ✅ **P4-2 HARDENED 2026-06-18** (4-agent adversarial Opus review): fixed negative-denominator sign-flip, carry expiry now from the scheduled contract code (resolved a 1-3mo recent-carry **staleness** bug + roll-time hindsight; coverage now to 2026-06-17), carry cross-section guards (std/min-width/ffill-cap/cost-releverage), `pct_change` deprecation (live trend unchanged 0.724), universe micros/STIRs by NAME (73→**76** markets; real bonds kept). Survivorship flag **quantified immaterial** (full 0.66 vs ≤2005-history 0.65). **Verdict CONFIRMED**: carry PAPER-PASS (point_SR 0.81), post-2015 +0.89, Track-B dSR +0.17. +6 real regression tests; 22 futures + 224 sleeve tests green. **DEPLOY: add CARRY to the live ETF-trend book (0.72→0.89); do NOT add futures-trend (decayed + redundant, →0.57).** ✅ **P4-2c REBALANCE-CADENCE STUDY 2026-06-19 → keep WEEKLY for both.** Added a backward-compatible no-trade `rebalance_band` to the TSMOM + carry engines (default off; live unchanged) + a pre-registered study. Trend: weekly +0.83 ≫ daily/band ~0.50 (whipsaw). Carry: daily wins full-sample (0.72>0.66) but ONLY via the early period — **weekly beats daily in the modern era (post-2015 0.89>0.79, 2020s 0.62>0.44)** so the daily edge isn't deployment-robust; band underperforms. 6 tests; 281 regression tests green. The full-sample daily-carry number was a non-robust artifact. **NEXT: report-only live-paper OOS tracker for the ETF-trend+carry book (weekly).** See DECISIONS / ML_EXPERIMENT_LOG 2026-06-18 (P4-2 + hardening) + 2026-06-19 (P4-2c).

---

## 🧭 PRIOR (2026-06-14): Alpha-v8 Research Program (Overlay & Timing track) — COMPLETE — executing G0 next

**New research program set:** [`docs/reference/ALPHA_V8_RESEARCH_PLAN.md`](../reference/ALPHA_V8_RESEARCH_PLAN.md) (Opus-architected). Three free-data angles, phased + exhaustively tested sequentially: **G0** overlay marginal-stacking infra → **G1** credit/curve de-risk overlay (highest EV) → **G2** short-interest overlay (power-gated) → **G3** additive timing sleeve (hardest) → **G4** owner-gated live wiring. Built on the F-series lesson: overlays work, additive equity sleeves hit the corr/IC wall; pre-registered both-halves stability guard + Opus deep-dive are mandatory. **All G0–G4 DONE & merged.** The VIX governor live + carry-kill + CI work (below) are DONE & verified.

---

## 🧭 PRIOR (2026-06-14): Alpha-v7 F-series done + BOTH owner decisions actioned — carry KILLED; ⭐ VIX governor WIRED LIVE (verified running)

**Owner actioned both pending decisions (2026-06-14):**
- ✅ **Carry confirmation → KILLED** (`F3-CARRY-CONFIRM-20260614`): edge is a pre-2016 artifact (H1 SR +0.69 vs H2 −0.10); the pre-registered stability guard caught it. Line closed.
- ⭐ **VIX-term crash governor → WIRED INTO THE LIVE TREND SLEEVE** (owner-approved). `app/live_trading/trend_sleeve.py`: `alloc *= _crash_governor_multiplier(db)` — de-risk to 0.5 on VIX>VIX3M backwardation. Flag `pm.crash_governor_enabled` ON by default, reversible live; FAIL-SAFE (any data problem → 1.0 = today's behavior); PIT-correct (settled closes); can only reduce exposure. Opus pre-merge review (1 HIGH PIT look-ahead found + fixed → SHIP). 50 tests. **⚠️ DEPLOY: the orchestrator/scheduler process must be RESTARTED to load the new code before the governor takes effect** (flags need no restart). Until then live behavior unchanged.
- Plus **CI hardened** (#490): flaky training-test timeout (serial-path) + NFP test made hermetic → first 0-fail local suite.



**Owner gave go-ahead to execute the full F0→F5 plan autonomously (Opus each step; document/email/merge; promote-to-live stays owner-gated). Done — every phase built/tested/Opus-reviewed/merged or explicitly deferred. Final scoreboard:**
- **F0 Sleeve Lab — MERGED (#484):** the uniform, hardened pipeline + overlay path + registry. **Permanent infra** (made this whole sweep ~20-line declarations).
- **F1a turn_of_month + overnight — FAIL (#485):** miss significance + don't diversify (timed SPY beta).
- **F1b ⭐ VIX-term crash governor — MODESTLY HELPS (#486):** maxDD −13.9→−12.1%, Calmar 0.469→0.501, Sharpe flat, COVID DD −10.7→−6.5%; ~0.5%/yr give-up. Overlay candidate. NOT promoted.
- **F1c FOMC drift — DEFERRED:** additive-SPY-beta shape + date-list bug risk.
- **F2 ETF relative-value — FAIL (#487):** orthogonal (corr −0.23) but ~zero standalone edge. *Learning: standalone return, not orthogonality, is binding.*
- **F3 rates carry — NEAR-MISS (#488) → CONFIRMATION KILLED (#491):** owner-authorized fresh pre-registered confirmation `F3-CARRY-CONFIRM-20260614` (long-flat IEF). Full-sample Track-B PASS (IR +0.434, P(ΔSR>0) 0.930) but Track-A still fails (HAC p 0.083) and the **pre-registered sub-period stability guard FAILED — H1 2007-2016 SR +0.689 vs H2 2017-2026 SR −0.098** (edge is a pre-2016 artifact, dead in the modern regime). **KILLED, line closed.** The stability guard caught a config-robust but time-unstable edge.
- **F4 options-conditioned events — DEFERRED:** already killed in P4 (H4a–H4e ALL KILL + PEAD demoted) on the same underpowered 4y options data; negative-EV re-tread.
- **F5 book assembly — NOT TRIGGERED:** 0 sleeves passed. Illustrative owner-pending book computed (trend+carry+governor: maxDD −13.9→−10.9%, SR ~flat, corr trend~carry +0.029).

**Bottom line:** no additive sleeve cleared the pre-registered bar on free daily US data — confirmed twice now (the carry confirmation's stability guard killed the last candidate). The trend sleeve remains the only standalone edge; the VIX governor (overlay) is the only surviving add.

**OWNER DECISIONS — both actioned (2026-06-14):**
- ✅ **#2 carry confirmation — DONE → KILLED** (edge doesn't persist post-2016; line closed).
- 🔧 **#1 adopt the VIX-term crash governor as a live overlay — IN PROGRESS** (owner said yes; wiring it into live sizing now, flag-gated + tested + Opus-reviewed).

Once the governor is live, the next lever (if more alpha is wanted) is a deliberate DATA buy (Norgate futures / longer options history), not more free-data searching. Live book currently unchanged (trend-only 25% + cash) until the governor PR lands.

---

## 🧭 PRIOR (2026-06-14, earlier): 5-LLM research panel synthesized → game plan set (Sleeve Lab + orthogonal deep-history premia)

**The candidate sweep found no new model → went back out to 5 external quant LLMs (Opus 4.8, ChatGPT, DeepSeek, Gemini, Grok) for next-steps; all 5 returned; synthesized into the new direction SSOT: [`docs/reference/ALPHA_V7_RESEARCH_SYNTHESIS_2026-06-14.md`](../reference/ALPHA_V7_RESEARCH_SYNTHESIS_2026-06-14.md).** Inputs archived at `docs/archive/llm-reviews/2026-06-14/`. Unanimous picture: kills are honest, trend is the only edge, build a 3–5 sleeve premia book around trend at a realistic **book SR 0.7–0.9** (not a home run), on **deep free history** (19y ETFs / FRED). **Owner game plan (phased, see SSOT + MASTER_BACKLOG):** **F0** build a future-proof **Sleeve Lab** (uniform tested sleeve→Ruler-v2(A+B)→allocator→report pipeline + registry; retire bespoke `run_*_cpcv` scripts) · **F1** structural/calendar/overnight premia + a VIX-term crash governor (most orthogonal/powered/cheap) · **F2** slow ETF relative-value · **F3** carry-done-right (small) · **F4** options-conditioned event interaction (long shot) · **F5** book + live fidelity. Deferred/data-gated: cross-asset trend via futures (Norgate), aggregate short-interest timing (FINRA backfill), index-VRP ETP. **No code/live change yet — this is the plan; execution starts at F0 on owner go-ahead. Live book unchanged (trend-only 25% + cash).**

---

## 🧭 PRIOR (2026-06-14): Ran the available candidates through the LIVE Ruler-v2 gate → NO new paper model

**Both Ruler-v2 gates are live (Track-A `GATE_MODE` + Track-B `TRACKB_MODE` = `ruler_v2`).** With the better (less-Type-II) gate live, ran the two data-complete candidates through it to hunt for a turn-on-able PAPER sleeve. **Result: nothing new passes — every available candidate genuinely fails** (independent Opus methodology review: honest negatives, not scoring artifacts; ML_EXPERIMENT_LOG 2026-06-14):
- **P5 broadened trend** re-scored (regime WAIVED = best case): PAPER FAIL — OOS point SR 0.123 (<0.30), HAC p 0.30 — failed on significance, NOT regime → genuinely weaker than the simple live sleeve, not a Type-II.
- **Index short-vol / VRP**: PAPER FAIL — Mean Sharpe −0.207 (1×) / −0.426 (2×), negative over 2022-2026; Track-B-vs-trend is MOOT (a negative-SR sleeve can't pass the standalone-SR>0.20 backstop).
- H4 options (significantly negative), PEAD (demoted t=−0.77) stay dead; a from-scratch carry/clean-VRP premia sleeve is **unbuilt**.

**The live 10-ETF trend sleeve (25% + cash) remains the only validated edge.** This is the gate working correctly (candidates failed on real significance/SR with the regime backstop waived), not a false negative. A genuinely new edge needs NEW data (longer VRP history / new trend asset classes) or NEW strategy code — a research-investment decision, not a re-run. Tooling: `scripts/run_trend_broaden_rulerv2.py`; fixed a cp1252 console crash in `CPCVResult.print()`.

---

## 🧭 PRIOR (2026-06-12 → 06-13): Alpha-v7 Phase B — Ruler v2 built, R4-remediated, and TAKEN LIVE

**Phase 2 (PR #472, dark) wires the inference core into a two-tier gate behind `GATE_MODE="ruler_v2"`** (not flipped; legacy gates untouched, 89 tests green). `bayes_sr.py` = the closed-form Bayesian posterior `P(SR>0)` that replaces the saturated DSR (registry trial-count shrinks a mean-zero prior; backtest + live-paper combine precision-weighted). `ruler_v2.py` = PAPER (plausibility: point-SR floor + implausibility ceiling + non-catastrophic regime + sleeve cap; PF/Calmar demoted to report-only) vs CAPITAL (significance: Bayesian posterior ≥0.95 + multi-factor residual-α t + bootstrap + PBO-if-M>1 + hard power floor). `CPCVResult.oos_returns_dated` (pure-additive) carries the dated OOS book series the gate runs inference on. **The independent Opus deep-dive caught a CRITICAL** — CAPITAL was "unreachable on a backtest alone" only by threshold luck (a clean t≈3 backtest cleared 0.95 ~1-in-5 draws); fixed by making **live-paper a STRUCTURAL gating criterion** (capital ⇒ posterior must include a live-paper observation), plus the missing `run_cpcv` population integration test. **Phase 3 (PR #473, dark) — Track-B v2** (`scripts/walkforward/track_b_appraisal.py`, behind `TRACKB_MODE="ruler_v2"`): replaces the budget-DEPENDENT legacy ΔSharpe≥0.10 bar (which secretly encoded a position size) with the **budget-invariant appraisal ratio** (residual-α IR via `multifactor_alpha(cand_vt, factors={base})`) + **block-bootstrap P(ΔSR>0)**. Worst-regime floor waived for declared diversifiers/risk-premia; fails closed for other components lacking regime data (unless explicitly waived). Opus deep-dive verified budget-invariance empirically + ruled out junk-sleeve gaming (2 MINOR fixed). Legacy `book_delta_gate` untouched. **2026-06-13 — OD decisions ratified (#475) + R4 calibration run → NOT CLEAN → DO NOT FLIP.** Ratified OD-1…OD-9 (independent Opus advisory): R7 trial-count fix, OD-9 premia factor policy (trend excludes TSMOM), OD-5 PdSR→0.90, OD-2 documented. Built the R4 pre-flip instrument (report-only `rv2_*` columns in `gate_calibration.py`, #476) and ran the decisive real controls. **R4 NOT CLEAN** (artifact `logs/gate_calibration_20260613.json`; independent Opus-reviewed): (A) the tsmom positives failed Ruler-v2 PAPER on the regime backstop — a RUN ARTIFACT (a crisis-diversifier scored through Track-A instead of Track-B + thin stress-regime coverage), and (B) a TRUE null (random_balanced_seed_5) PASSED Ruler-v2 PAPER — a REAL Type-I leak (the 0.30 plausibility floor admits ~23% of zero-edge nulls at n≈1500). **GATE_MODE/TRACKB_MODE NOT flipped** (the validation gate correctly blocked the flip). **2026-06-13 (later) — remediation (1)+(2) DONE → R4 now CLEAN** (DECISIONS 2026-06-13 later): (1) ruler_v2 PAPER waives the worst-regime backstop for declared diversifiers/risk-premia (`CPCVResult.component_type`; tsmom→`risk_premium`) → both tsmom positives now PAPER-pass; (2) ruler_v2 PAPER adds a light one-sided HAC-SR significance floor (`RULERV2_PAPER_MAX_HAC_P=0.05`, pooled-OOS instrument) → all 5 true-nulls now PAPER-fail incl. the lucky `seed_5` (leak closed; MC 5.7% null-pass vs 23.4%). Opus deep-dive=SHIP. **R4 RULER-v2 CHECK = CLEAN** on the decisive controls (positives clear, nulls dead, leaky rejected). **2026-06-13 (full set) — ran pead_baseline + xmom_12_1: R4 NOT strictly clean — `xmom_12_1` (labeled positive) FAILED ruler_v2 PAPER.** On the merits this is a CORRECT rejection (xmom 12-1 is genuinely insignificant now — meanSR 0.17, t 0.77; the legacy gate fails it too; cross-sectional momentum was ruled dead 2026-06-03). **DID NOT flip.** A drafted R4 carve-out that would have made it read CLEAN was caught by an independent Opus methodology review as GOALPOST-MOVING (post-hoc, single-control-targeted, and used the discredited path-t to define "significant") → **reverted**. The flip went back to the owner, who chose (b) reclassify xmom → known_marginal, then flip. **2026-06-13 GO-LIVE (#479): `GATE_MODE` flipped `significance`→`ruler_v2` — Ruler v2 is now the production Track-A gate.** R4 came back strictly CLEAN on the full set (no carve-out; xmom now known_marginal); independent Opus go-live audit confirmed mechanically-correct + no live break + reversible; gate_calibration decoupled from GATE_MODE; full suite under the flip = only the known local-only NFP failure. **`TRACKB_MODE` is also now `ruler_v2` (2026-06-13, #480)** — the run_book_gate dispatcher was wired (the go-live audit had caught that flipping TRACKB alone would be inert); a live tsmom-vs-PEAD appraisal returns PASS (IR +0.881, P(ΔSR>0) 0.976, regime waived). run_book_gate stays a manual, report-only, owner-gated tool; legacy `book_delta` retained. The NEXT retrain/promotion is gated by Ruler v2; **no promotion is pending — live book unchanged (trend-only 25% + cash).** Both Ruler-v2 gates (Track-A `GATE_MODE` + Track-B `TRACKB_MODE`) are now live. See DECISIONS 2026-06-13 (GO-LIVE + Track-B GO-LIVE), PIPELINE_ARCHITECTURE §7.0 + changelog, MODEL_STATUS.

**Phase 5 (PR #474, REPORT-ONLY) — kill-ledger re-score** (`scripts/walkforward/ruler_v2_rescore.py`): tabulates the PAPER-tier flip (REVIVED / DEMOTED / unchanged) of each CPCVResult under Ruler v2 vs its recorded significance verdict — the question Alpha-v7 exists to answer (which significance-kills a less-Type-II plausibility ruler would revive). HARD CONTRACT: never flips a gate, never writes the registry, never promotes — a REVIVED row is a CANDIDATE for an owner-initiated, R4-logged re-test, not an action. Opus deep-dive caught + fixed a CRITICAL (the re-score mutated the caller's result + a swallowed-exception spurious-REVIVED path). **Phase B build is COMPLETE (Phases 1-3+5 all merged, all DARK/report-only).** **Remaining = owner calls: (a) OD-1…OD-9 sign-off, (b) the live `GATE_MODE="ruler_v2"` / `TRACKB_MODE="ruler_v2"` flip, (c) reviewing any REVIVED rows from running the re-score on the real ledger.** Live book unchanged (trend-only 25% + cash).

---

**Phase 1 (PR #471, dark) — the pure inference keystone.** `app/research/inference.py` is the pure inference keystone (numpy/scipy in, frozen dataclasses out, no I/O, no gate wiring): `hac_sharpe` (autocorrelation-robust SR>0 significance, Lo-2002 mean-HAC t), `stationary_bootstrap_sr` (Politis-Romano P(SR>0)+CI, data-driven block length), `pbo_cscv` (Bailey/LdP Probability of Backtest Overfitting via CSCV), and the canonical `multifactor_alpha` (moved here from `options_xs_ls.py`, which now re-exports it). Built Opus 4.8 → **two independent Opus deep-dives**: round 1 fixed the SE labeling (`se_sr_ann_implied` — it's the mean-HAC t, not Lo's full SR SE) + integrated-autocorrelation-time block length + a flaky null test; round 2 caught three contract/bias issues feeding a future capital gate — `multifactor_alpha` raised on an inf factor (`.dropna()` doesn't drop ±inf), PBO silently corrupted to a finite gating number on a NaN perf cell, and PBO's `<=` rank biased PBO DOWN (optimistic) under ties — all fixed (finite-mask, fail-closed, tie-fair mid-rank). 22 known-answer tests; round-2 verification = **SHIP**. **Next: Phase 2** — persist `CPCVResult.oos_returns_dated` + `ruler_v2.py` + `bayes_sr.py` behind a `GATE_MODE="ruler_v2"` flag (coexists with legacy; zero diffs at default). Owner decisions OD-1…OD-9 needed at Phase 2/3, NOT Phase 1.

## 🧭 EARLIER TODAY (2026-06-12): H1 verdict in — PEAD is NOT an event-level edge → book = trend-plus-cash

**The centerpiece answered the quarter's most important question.** H1 (`H1-PEAD-EVENTLEVEL-20260611`, one-shot R4, `panel_sha256=af206149…`) re-adjudicated the LIVE PEAD edge at the EVENT level on a 21,330-event / 9,774-qualified R1K panel (2019→2026) with two-way (announce_date×firm) CGM-clustered SEs (validated to the published Petersen-2009 pins): **PRIMARY 10d SPY-hedged mean −8.3bp, t=−0.77, one-sided p=0.7804 → DEMOTE.** Negative at every horizon; conservative bootstrap p=0.66; robust to all LOCO; the announce+1-vs-+2 gap is +2.6bp (negative even at the favorable entry). **PEAD's case for capital is closed** (corroborates Alpha-v4 Phase-1's CAPM hedged-Sharpe −0.37). The inference-upgrade success metric is met: no strategy is killed/kept on an 8-fold t-stat again.

**▶️ OWNER ACTIONS — status (live config verified 2026-06-12):**
1. ✅ **DONE — live PEAD sleeve flipped OFF** (overnight; notify id 59). Mechanism: `pm.swing_selector` `'pead'`→`'ml_model'` (+ `pm.swing_ml_live_enabled='false'`) → the swing/PEAD proposing path runs the dormant dead-ML branch and fires nothing; PEAD no longer trades. `pm.pead_size_mult` stays 1.0 (moot — PEAD isn't the selector; the pead_tracker telemetry is retained). **Live book = trend-only (`trend_enabled='true'`, `trend_shadow='false'`, 40%) + cash.**
2. ✅ **DONE (2026-06-12) — live trend weight reconciled 40%→25%** (`pm.trend_allocation_pct` 0.4→**0.25**; Track-B 25% framing, #451). Trend is the sole live sleeve; the new 25% gross applies from Monday's first real rebalance. Schema default + `set_trend_config` + docstrings brought in line. (`pm.allocator_enabled='false'`.)
3. ✅ **DONE — uvicorn restarted** (2026-06-12 07:52) → notify_watcher live (drained the queue incl. the P4a email), clean logs + FMP calendar (#445/#446), and the 15:55 ET NBBO snapshot logger (#455) active.
4. **H2/H3 are NOT auto-run** — they were PEAD-improvement hypotheses; with PEAD demoted, whether to still run them as pure research (no live capital) is your call. The event-panel + CGM instrument is the standing tool for any future event hypothesis.

**Also shipped 2026-06-12 — Alpha-v6 P4a (options-as-signal DATA LAYER, no verdict yet):** the daily options **feature table** (`app/data/options_features.py` + `scripts/build_options_features.py` → `data/options_features.parquet`: CPIV / 25Δ-skew / term-slope / IV-RV / O-S volume, PIT, holiday-aware knowable_date + split-adjusted RV) + the **options-quality universe filter** (`app/data/options_quality.py`), and **H4a–H4e PRE-REGISTERED** (`preregistered_at=2026-06-12T12:00Z`; kill = simple decile sorts net-of-costs show nothing → CLOSE, NOT an XS-ML revival). Built Opus 4.8 → independent Opus 4.8 deep-dive (**2 PIT/split BLOCKERs caught + fixed before any run**) → Opus verification = SHIP; 47 tests.

**Also shipped 2026-06-12 — Alpha-v6 P4 H4a–H4e VERDICTS → ❌ ALL 5 KILL (options-as-signal equity edge DEAD):** the five confirmatory R4 runs (`scripts/run_options_xs_cpcv.py`, full 4y/208wk decile L/S) all KILL — H4a CPIV t=−2.70, H4b skew −4.10, H4c put-O/S −4.43, H4d term-slope −2.83 (all significantly NEGATIVE — academic signs don't hold 2022–26 at equity cost), H4e IV/RV −0.12 (noise). The inverse is NOT tradeable (post-hoc sign-mining; Opus deep-dive = 2022–23 growth-crash regime). Independent Opus 4.8 deep-dive confirmed NO look-ahead + fixed a decision-label BLOCKER and the monotonicity Type-II trap before recording; 29 tests. **Options confirmed (again) as a data asset, not a tradeable equity signal; live book unchanged (trend-only 25% + cash).**

**Also shipped 2026-06-12 — H2/H3 (event-conditioned) adjudicated:** built the **event-time options join** (`scripts/enrich_event_panel_options.py` + `app/research/event_options_join.py`) populating the event panel's options-pre-event features (PIT, knowable_date-gated; ~45% coverage; Opus deep-dive SAFE-TO-RECORD). **H2 → NOT_CONFIRMED** (reaction_ratio continuous: 10d t=−1.21, no significant edge → **OPT-5 settled, stays PARKED**, no threshold-hunting). **H3 → BLOCKED** (its frozen feature list needs `revision_momentum` = forward-estimate-revision data, which is DATA-BLOCKED; the runner refuses to consume the one-shot — H3's pre-registration stands for if that data is ever acquired). **All Alpha-v6 confirmatory hypotheses (H1, H2, H3, H4a–e) are now adjudicated.** Live book unchanged.

**Also shipped this session (all CI-merged):** P0 stubs + H1/H2/H3 pre-registration (#454); both slow fuses (#455) — the **Alpaca NBBO logger** (Polygon serves no options NBBO → free Alpaca feed; day-1 captured) and the **computed-greeks store COMPLETE** (`data/options_greeks/`, 733/733, 112.8M rows). Process held throughout: Fable-5 build → independent Fable-5 adversarial review → fix loop; **3 BLOCKERs caught before merge/run** (event-regime gate units, yfinance split-adjustment, empty-pre crash).

## 🧭 PRIOR (2026-06-11): Alpha-v6 Phase-0 machinery COMPLETE — lead with P3 (earnings-event panel / H1)

**Shipped overnight (8 PRs, all CI-merged; main @ `ab8151c`):** the P0 *measurement* machinery is built + validated end-to-end —
- **Gate-calibration harness** (#444) + result (#447): the gate's false-negative is the **worst-regime backstop, NOT the t-stat**. "Lower the t-bar" is empirically **REFUTED** — 3/5 TRUE zero-SR nulls clear t≥2.0, and PEAD's t=3.33 ≈ a noise null's 3.47 (the 8-fold path-t can't separate PEAD from noise).
- **Two-track Track B book-delta gate** (#448) — judges diversifiers/risk-premia on book contribution, not the standalone floor.
- **Research registry / pre-registration ledger** (#449) — the program's true N_TRIALS (DSR demoted to report-only).
- **First real Track B run** (#450) + **owner-approved REGISTERED budget amendment 10%→25%** (#451) → **TSMOM PASSES Track B** (Sharpe 0.41→0.64; sweep crossover ~12.5%, so 25% is in the pass region with margin). TSMOM is now Track-B-validated (trend is already live at 40% capital; reconciling the live weight to the 25% *risk* framing is a SEPARATE owner call).
- **Ops:** uvicorn `[uvicorn.error]`→`[uvicorn]` (#445); FMP econ-calendar fixed (#446 — it was a **dead legacy `/api/v3`**, NOT a paywall; migrated to `/stable/`, works on the $29 plan) + new `docs/reference/DATA_PROVIDERS.md`; stale `risk.peak_equity` $50k→$101k reset.

**▶️ NEXT DIRECTION (Fable 5 recommendation, ADOPTED): LEAD WITH P3 — the earnings-event panel — breaking the blueprint's "P3-after-P2" dependency.** Why: P0 proved the path-t can't separate PEAD from noise, and **PEAD is live capital on that broken instrument**; event-level inference (H1) is the replacement and needs **ZERO options features** → it is NOT blocked on P2.
- **✅ DONE 2026-06-11 (PR #454) — P0 stubs (1)+(2) + pre-registration (3 below):** `--hypothesis-id` registry enforcement wired into all 9 `run_*_cpcv` scripts (shared `registry_enforcement.py`; warn-only until **2026-06-25** then required, or `--exploratory`); `event_regime_sharpes()` built + surfaced as `CPCVResult.event_worst_regime_sharpe` — **REPORT-ONLY (does NOT gate; waiver retirement deferred to H1/P3** — a per-event Sharpe isn't unit-comparable to the annualized-daily −0.5 floor, and retiring the waiver is H1's pre-registered consequence; the Fable-5 review caught + I reverted a first-cut gate-fallback). **H1/H2/H3 PRE-REGISTERED** (frozen criteria, `preregistered_at=2026-06-11T12:00Z`): `H1-PEAD-EVENTLEVEL-20260611`, `H2-IMPLIEDMOVE-CONTINUOUS-20260611`, `H3-PEADV2-SCORECARD-20260611`. See DECISIONS / ML_EXPERIMENT_LOG / PIPELINE_ARCHITECTURE §7.0c (2026-06-11).
- **✅ DONE 2026-06-11 (PR #455) — both slow fuses LIT:** (P1c) `scripts/log_options_nbbo.py` — nightly 15:55 ET NBBO snapshot logger via **Alpaca** (`feed=indicative`, free) because the **Polygon $79 plan serves NO options NBBO** (verified: `/v3/quotes` → 403; snapshot has no `last_quote`/IV/greeks). ⚠️ **The recurring schedule activates only on a uvicorn restart** (owner) — until then it can be run manually. (P2) `scripts/backfill_computed_greeks.py` — STARTED (background) → `data/options_greeks/`; European-warm-start American refine (~0.6h; naive was ~122h — a put-IV CRR-degenerate landmine). Inputs are **as-traded** (Polygon `adjusted=false` closes — yfinance is split-adjusted, a BLOCKER the Fable-5 review caught: ~2.9M rows would have had S wrong by the split ratio). 2× Fable-5 review (1 BLOCKER + 6 fixes). Fitter/CalibratedSpreadModel deferred (~4-6 wks of NBBO needed).
- **▶️ NEXT (this session):** (PR3) build **`app/research/event_panel.py`** (equity cols, 2019→2026) + **`scripts/walkforward/event_inference.py`** (two-way cluster-robust OLS, Cameron-Gelbach-Miller, unit-tested vs a published reference) → **run H1** under `H1-PEAD-EVENTLEVEL-20260611`. Pre-committed acceptance: p<0.05 → PEAD graduates to an honest Track-A paper pass (no waiver); p>0.15 → live book = trend-plus-cash (trend is the capital base either way). H1 may well **demote** PEAD (Phase-1 CAPM had hedged Sharpe −0.37) — that's the decision the live book needs.

**Process (standing):** Fable 5 (model:fable) for design/analysis/implementation → INDEPENDENT Fable-5 deep-dive for bugs → fix + re-review until perfect → tests → NO-DRIFT docs → email phase_complete → CI-gated merge. One branch/PR at a time, squash-merge, delete branch, sync main.

**Guardrails (what NOT to do):** don't touch significance thresholds (refuted); no new Track-B sleeves / index-VRP re-test on the TSMOM-pass high (preconditions unmet + cooling-off); no more binary threshold sweeps (the OPT-5 trap) — the `run_pead_implied_threshold_sweep.py` tooling was REMOVED (#452; H2's continuous pre-registered coefficient is the sanctioned settlement; the OPT-5 FRAGILE verdict stays logged). Don't start P5 trend-broadening until Monday's first real rebalance + the trend replay-diff are boring.

**Live ops:** first REAL trend rebalance **Mon 2026-06-15 09:45 ET** (unchanged). ⚠️ **Restart uvicorn** to pick up #445/#446 (cleaner logs + the working FMP calendar). SSOT for the full plan: [NEXT_PHASE_BLUEPRINT_2026-06.md](../reference/NEXT_PHASE_BLUEPRINT_2026-06.md).

---

## 🧭 PRIOR (2026-06-10): operating the live book; options = data asset (not a sleeve); external review out
- **Live book unchanged = PEAD (telemetry size) + TSMOM trend.** First REAL trend paper rebalance still **Mon 2026-06-15 09:45 ET** — verified READY (read-only shadow sim) and reliability-hardened (per-order commit + 1800s weekly misfire grace, #430). Today is quiet/no-trades and that's correct: swing ML ranker dormant by design (`pm.swing_ml_live_enabled=false`), PEAD found 0 qualifying earnings signals, trend not due until Monday.
- **OPT-5 implied-move filter → ❌ FRAGILE / PARKED (#433).** The threshold-robustness sweep showed the PEAD lift exists only at ratio=1.0 and *inverts* at 1.25 → overfit-suspect. Don't pursue without a powered + pre-registered re-test. Broader options program stays PAUSED (short-vol = risk premium, not alpha).
- **Options data → full 4y local store acquired (#440).** 2022-06-09 → 2026-06-08, ~112.8M bars, 733 names, ~6.18M contracts — the max the Polygon Developer plan serves. We now own the complete copy **even if the subscription is cancelled.**
- **Reliability / test-infra hardening (this session):** kill-switch strict-bool restore (#434); test→prod log/DB bleed closed via one shared `is_test_mode()` (#435/#436); weekly regime retrain + fixed 3-class gate revived from an abandoned PR (#439); Finnhub+FMP economic-calendar 403 log-spam silenced (#437/#441 — the calendar needs a paid tier on BOTH providers; falls back to FRED macro + hardcoded FOMC/NFP).
- **External quant review prepped:** `docs/reference/prompts/EXTERNAL_QUANT_REVIEW_PROMPT.md` + the `20260610_Quant_Options_Review/` kit — soliciting world-class-quant feedback on the next phase across multiple LLMs (harness soundness, options models, architecture gaps).
- **✅ DIRECTION CHOSEN → ALPHA-v6** (SSOT: [NEXT_PHASE_BLUEPRINT_2026-06.md](../reference/NEXT_PHASE_BLUEPRINT_2026-06.md)). The 5-LLM synthesis (Gemini/DeepSeek/Grok/ChatGPT/Claude, deep-dived by Fable 5, code-grounded) converged: our harness over-corrected into a **Type-II / false-negative machine** — a t≥2.0 gate on ~8 folds of ≤4y data kills *true* Sharpe-0.5–0.7 edges (t≈SR·√years), so 100% KILL (incl. confirmed-real index VRP) is a miscalibrated ruler, not an empty opportunity set. **7-phase plan:** **P0** calibrate the ruler (gate positive/negative controls — TSMOM-on-4y decisive — + two-track acceptance [alpha vs book-delta] + research registry) → **P1** live-book fidelity (replay-diff / fill-quality / NBBO spread-calibration) ∥ **P2** options *feature* layer (persisted greeks / surface-quality / BMO-AMC snapshots) → **P3 (centerpiece)** earnings-event panel + event-LEVEL PEAD inference + PEAD v2 continuous score → **P4** options-as-equity-signal XS sleeve (CPIV/skew/O-S/term-slope) → **P5** trend broadening (19y) → **P6** index-VRP micro-sleeve behind the book gate. **Options = signal-first, not execution.** Reviews archived under `docs/reference/prompts/20260610_Quant_Options_Review/responses/`.
- **▶️ P0 STARTED — gate-calibration harness SHIPPED (PR #444):** `scripts/walkforward/gate_calibration.py` scores positive (`tsmom_4y` decisive, `tsmom_19y`, `xmom_12_1`, `pead_baseline`, `spy_buyhold`) + negative (balanced/beta nulls, `leaky_tplus1`) controls through the PRODUCTION gate to MEASURE its false-negative/false-positive rates. Built + 2× Fable-5 adversarial review (1 BLOCKER + 3 MAJOR fixed), 49 tests, changes no threshold. **✅ RESULT (full 16-control run): the gate IS a false-negative machine for our edges (0/4 positives pass PAPER) — but the t-stat is NOT the cause.** `tsmom_4y` posts t=6.72 and fails ONLY on the `worst_regime_sharpe` backstop; meanwhile 3/5 TRUE zero-SR nulls clear t≥2.0 (so lowering t* admits noise → pre-registered rule verdict `NO_ADMISSIBLE_TSTAR`). PEAD's t=3.33 ≈ a noise null. **✅ Two-track acceptance gate (Track B book-delta) SHIPPED (#448)** — `book_gate.py` judges diversifiers/risk-premia on book contribution (not the worst-regime floor); 8 pre-registered criteria incl. the registered tail-overlap test; 2× Fable-5 review (3 MAJOR fixed). **✅ Research registry (pre-registration ledger) SHIPPED (#449)** — `app/research/registry.py` enforces pre-registration integrity (the program's true N_TRIALS; DSR is report-only); 2× Fable-5 review (1 BLOCKER + 3 MAJOR fixed), 45 tests. **✅ FIRST Track B run + registered amendment done (#450, #451): TSMOM now PASSES Track B.** First run at the 10% budget FAILED only on ΔSharpe (the bar structurally rejects any diversifier at that thin slice). Owner-approved REGISTERED amendment raised the risk budget 10%→25%; re-run → **TSMOM PASSES all 8 criteria (Sharpe 0.41→0.64, Calmar 0.28→0.59, maxDD shallower)**; budget sweep crossover ~12.5%. TSMOM is now **Track-B-eligible for paper book inclusion** (actually wiring it into the live book at a 25% weight is a SEPARATE owner call). **NEXT P0: wire `--hypothesis-id` into the run scripts + `event_regime_sharpes()`; then bring forward event-level inference (P3).** See DECISIONS 2026-06-11.
- **Operate Monday's first real trend rebalance** (still Mon 2026-06-15 09:45 ET) in parallel.

---

## 🧭 PRIOR (2026-06-09): options program PAUSED; operating the validated live book
- **Live book = PEAD (telemetry size) + TSMOM trend.** The **dead cross-sectional swing ML ranker is now OFF in the live path** (`pm.swing_ml_live_enabled=false`) — it was silently producing ~30/32 recent trades of a validated-null strategy (#418). The Trader's ML-score gate is now observable (`REJECTED_ML_SCORE`).
- **Trend armed for paper:** `pm.trend_enabled=true`, `pm.trend_shadow=false` → first REAL paper rebalance **Mon 2026-06-15 09:45 ET** (weekly cadence, ~7 ETFs, 40% alloc), via the orchestrator scheduler.
- **PEAD** clears the 0.55 entry gate (confidence 0.65–0.90); event-sparse (fires on earnings). Live-sizing fidelity fixed (sizes off PEAD's own ATR stop, not the swing stop).
- **Alpha-v5 Options Program (OPT-0..4): built, validated, PAUSED.** Two Opus-certified verdicts: single-name earnings IV-crush = KILL (cost-killed); index short-vol = KILL standalone but **VRP real + cost-robust** (PF 2.24/1.75), just Sharpe-weak. Key insight: our gate is an *alpha* gate, short-vol is a *risk premium* (wrong ruler). Harness retained; revisit later as a book diversifier with a risk-premium framework + more data. See DECISIONS 2026-06-09.
- **Next:** OPT-5 (options-data-as-signal: implied-move filter for PEAD + put-skew risk-off, judged on the host sleeve's gate — sanctioned parting options win, data kept) + continued live-book hardening. ⚠️ Recurring CI flake (`test_agent_simulator_rebalance.py` DB-lock) still costs a rerun on most PRs.

---

## 🧭 P3 (2026-06-08): Live regime-aware sleeve allocator — wired, shipped DISABLED
`app/live_trading/sleeve_allocator_live.py` turns the backtest allocator into a live,
kill-switchable book layer. **Ships `pm.allocator_enabled=false` → byte-identical to
today's static budgets** (trend 0.40 / PEAD telemetry). When enabled it recomputes weekly
(before the trend rebalance), persists effective sleeve weights to agent_config, and both
sleeves read them with a fixed-weight fallback (disabled/stale/warmup/error → static).
Default scheme `equal` (the validated winner); `vol`/`regime` are live-capable but OFF
until `scripts/run_book_allocator.py --emit-config` selects them (expected after the 3rd
sleeve). PEAD regime double-tilt guarded (Opus review). To enable later:
`python -m scripts.set_allocator_config --enable`. **NEXT: the 3rd uncorrelated sleeve**,
then re-run the gate to (likely) activate vol/regime.


---

## 🧭 ACTIVE DIRECTION (2026-06-06): Alpha-v4 — Portfolio of Uncorrelated Premia
**SSOT:** `docs/reference/QUANT_REVIEW_SYNTHESIS_2026-06.md` · **Index:** `MASTER_BACKLOG.md` (Alpha-v4 table).

Five independent world-class-quant LLM reviews (ChatGPT, Gemini, Grok, DeepSeek, Claude) converged: **the architecture is good — re-aim, don't rebuild.** The wall is the *opportunity set + a biased ruler*, not technique. Stop hunting one hero edge; assemble **~4 uncorrelated sleeves → book SR ≈ 0.8**, on an honest harness. PEAD is **not** proven alpha (p≈0.19, 87% P&L in up-trends = conditional beta) — keep small, pair with a crisis-positive sleeve.

**Locked (2026-06-06):** ① **PEAD → telemetry** (`pm.pead_size_mult` 3.0→**1.0**, `pm.pead_max_position_pct` 0.10→**0.05**, applied live restart-free — reverses B4). ② **Gates** → lower bar (~0.45) + reweight to robustness (residual-α-t + fold-consistency primary; keep survivability floor). ③ **Targeted re-arch** (keep execution; rework research harness; add sleeve+regime-allocator). ④ **Free-data only** until a spike justifies a paid purchase.

**Phases (EV/effort):** **P0** validation integrity (`is_trained` guard → full-coverage CPCV for rules-based; sequential-WF baseline; fold-coverage report; gate recalibration; freeze dead XS-ML) → **P1** PEAD reckoning (neutralization + FF5 attribution + gapper slippage; decision gate) → **P2** Trend/TSMOM ETF sleeve (crisis-diversifier, book-level eval) → **P3** regime-aware allocator (the unlock; must beat static weights net of turnover) → **P4** gated high-ceiling bets (PEAD 2.0 · options-VRP spike · squeeze-conditioning). **Regime policy:** "attribute, don't amputate" — switch *allocation* across full-history sleeves, never train a model per regime (§4b).

**PHASE 0.1 + PHASE 1 DONE (2026-06-06):** `is_trained` guard fix shipped (#394) → PEAD CPCV now full-coverage (skips 50%→8). On the honest harness PEAD is **NOT a standalone edge**: unbiased CPCV mean +0.578 but t=1.81 (<2)/p5=−0.796 (worse tail revealed); **CAPM beta-isolation is decisive — α=−1.29%/yr, HAC α t=−0.95, β=0.14; the beta-removed (market-hedged) Sharpe is −0.37** (hedge out SPY and it loses money). The positive Sharpe is small β riding the 2020–26 bull. Gapper-slippage collapses the edge by 50bps. → **PEAD = weak market-beta-driven risk-on satellite, kept at telemetry size (1.0×/5%, live), never a centerpiece.** Retires "PEAD is the sole edge." PEAD 2.0 dropped (was gated on neutralized PEAD showing life). See ML_EXPERIMENT_LOG Phase 1. Tool: `scripts/pead_phase1_attribution.py`.

**PHASE 2 DONE (2026-06-06) — TSMOM trend sleeve ✅ KEEP.** Built a vectorized PIT-safe TSMOM sleeve (`app/strategy/tsmom.py`, 10-ETF multi-asset, long-flat, inverse-vol, weekly) + validation (`scripts/run_tsmom.py`). Independent deep-dive: no look-ahead; fixed a cost-timing off-by-one. **Standalone Sharpe +0.714** (2007-26, incl. all crises), crisis-positive in *slow* bears (2008 +7% vs SPY −37%) but whipsawed in *fast* shocks (COVID −6%). **corr to PEAD +0.25** (diversifying). **Exit gate PASSES:** PEAD+trend (50/50, vol-matched) beats PEAD-alone on Sharpe (0.31→0.92) AND drawdown (−13.7%→−8.3%). Trend is the stronger sleeve; PEAD the weak satellite. See ML_EXPERIMENT_LOG Phase 2.

**PHASE 3 DONE (2026-06-06) — sleeve allocator built; SHIP SIMPLE FIXED-WEIGHT.** Built `app/strategy/sleeve_allocator.py` (general N-sleeve: equal/vol/regime, inverse-vol risk parity, regime hysteresis + EWMA blend, PIT) + `scripts/run_book_allocator.py`. Independent deep-dive CLEAN (no look-ahead). On the PEAD+trend overlap (2020-26): **equal-capital Sharpe +1.082 / maxDD −5.1% / 0× turnover** beats static vol-weight (+0.715) beats regime-tilt (+0.593 / −6.4% / 2.6×). **Regime tilt FAILS its margin** (worse on every metric) → regime layer OFF. **Inverse-vol is fooled by PEAD's sparse-low vol** (over-weights the weak sleeve) → loses to naive equal-capital. → **ship simple fixed-weight book; vol+regime layers kept as OFF scaffold; revisit with more sleeves / longer overlap.** ("Complexity must earn it.") See ML_EXPERIMENT_LOG Phase 3.

**ALPHA-v4 ARC (Phases 0-3) COMPLETE.** Net: PEAD = weak beta satellite (kept at telemetry size); TSMOM trend = the strongest sleeve (Sharpe +0.71 19y, diversifying); the book = the two at simple fixed weights (smart weighting doesn't yet earn its complexity on a 2-sleeve / thin overlap).

**✅ LIVE MULTI-SLEEVE WIRING SHIPPED (2026-06-06, shadow-first):** the trend sleeve now trades live as a **standalone weekly ETF rebalancer** (`app/live_trading/trend_sleeve.py`) alongside PEAD — NOT a `swing_selector` value. Direct Alpaca placement with a lightweight risk gate (kill-switch / gross cap trend+PEAD≤80% / fat-finger), fired by the orchestrator daily 09:45 ET → runs only on `pm.trend_rebalance_weekday` (Mon) when the market is open (fail-closed `get_clock`, covers holidays). **Equal-capital 50/50** (`pm.trend_allocation_pct=0.40`); PEAD dialed to telemetry (schema defaults rebaselined 1.0/0.05). Positions tagged `selector="trend"` and excluded from the Trader's stop/target loop (rebalancer-managed). Tracking via `trend_tracker.py` (+0.71 ref, weekly rollup). **Deploys DORMANT + SHADOW** (`pm.trend_enabled=false`, `pm.trend_shadow=true`). **To activate: restart uvicorn, then `python -m scripts.set_trend_config`** (set `pm.trend_enabled=true` for shadow; add `--arm` for real orders). 32 new tests pass; flake8 clean. **REMAINING (owner-gated):** verify a shadow run on the live data tier (≥253 daily bars per ETF), then arm.

**PRIOR NEXT (now done):** ~~live multi-sleeve wiring — get the trend sleeve trading at a simple fixed weight alongside PEAD~~. This places live ETF orders in the paper book → a deliberate live-system change needing owner go-ahead. Then **Phase 4** (options-VRP feasibility spike / squeeze-conditioning; PEAD 2.0 dropped after Phase 1). Phase 0 remainder (sequential-WF baseline, fold-coverage report, gate recalibration, freeze dead XS-ML) remains as harness hygiene. **CI `database is locked` flake — FIXED (2026-06-06):** per-worker SQLite isolation — feature_store / pead_tracker / notifier DB paths are now env-overridable and conftest points each xdist worker at its own temp dir (set before collection); the bare `universe_history` connect hardened (timeout=30). Validated under -n 8 (2767 passed, 0 locks).

---

## 🗄️ PRIOR DIRECTION (2026-06-02, superseded): Alpha v2 — `docs/living/ALPHA_V2_PLAN.md`
PEAD long-only is **live in paper**. The next phase is **structural, not signal-hunting** (from a 5-LLM external review + a code-grounded plan): de-risk PEAD honestly, then test whether the "dead" swing ranker was just strangled by a 5-position long-only book by re-running it **dollar-neutral, sector-neutral, high-breadth, residualized**. **Locked decisions:** short-interest data first; carve a post-2024-06-01 historical holdout; dollar-neutral shorting approved in paper; keep live PEAD running (pause only if leave-one-crisis-out fails). **First moves:** PEAD cost-sensitivity sweep → crisis-block robustness (leave-one-crisis-out). See the plan for the full sequenced roadmap.

**PHASE-1 PEAD DE-RISK COMPLETE (2026-06-02):** §1.1 cost ✅ GO · §1.2 crisis ✅ GO · §1.3 significance ❌ FAIL. PEAD is **real-but-underpowered** (event-level bootstrap p=0.19, HAC t=1.04; CPCV t=2.26 was optimistic) — a long-biased up-trend drift harvester (~0.40 SR, 87% P&L up-trends). **Keep paper-trading as a small diversifier; never a capital centerpiece** (CAPITAL-HOLD confirmed).

**🏁 RANKER VERDICT — DEAD (2026-06-03):** the §3.1 dollar-neutral high-breadth ranker (the Phase-2 capital hope) is **dead**, established rigorously. The first run was invalid (the "dollar-neutral" book actually ran ~35% net-long at 0.35 gross — the L/S engine was fed a one-sided long proposal pool and never re-sized held positions). Fixed end-to-end across 3 phases (observability → neutral-at-target-gross → full-cross-section scoring + breadth). On the **corrected, genuinely-neutral book** (net$ −0.01, gross 0.73), the decisive CPCV (k=8, N_eff=8) gave **Sharpe +0.14, t=0.18, %pos 67%, dep-adj +0.12** → **no cross-sectional alpha**; the long-only +0.22/+1.06 was confirmed **market beta**. Cross-sectional ML ranking is now exhausted (swing noise · intraday cost-drag · ranker null). **→ PEAD is the SOLE validated edge.** Also shipped this session: PEAD-aware entry gate (PEAD had been 0-filling — post-earnings gappers hit the swing 1.5%/0.5% thresholds; now live + filling) and PEAD cockpit v2 (live book + signal log + live-vs-backtest). **NEXT: strategy reassessment** — pivot research to the event-driven edge *family* (PEAD's DNA: discrete event → drift, F2-immune, rules-based) + productionize PEAD; the §3.3 short-interest / Spike-B residualization items are shelved (they were gated on ranker life). Alpha-v3 plan pending owner direction.

**🧭 ALPHA-v3 TRACK-A SWEEP COMPLETE — PEAD STILL SOLE EDGE (2026-06-03 PM):** owner approved Alpha-v3 (A: build a 2nd event edge; B: aggressively ramp PEAD in paper). Built the reusable **EventEdgeStrategy** harness (A0, PEAD byte-identical) + acquired **short-interest/short-volume data** (Polygon/FINRA, 540k rows, PIT-safe). Tested the two top candidates — **both NULL**: **A1 analyst up/downgrade drift** ❌ (CPCV looked best-in-campaign at +0.894/t=2.85 but was a **52% fold-skip artifact**; neutralized L/S +0.342/t=1.24, full-window CAPM alpha t=0.20 → noise) and **A2 dollar-neutral short-interest factor** ❌ (**−1.213, t=−3.53** — the Boehmer/Asquith anomaly *reversed* in the meme era). Cross-sectional ML ranker + A1 + A2 all dead. **PEAD (+0.546, real-but-underpowered) remains the SOLE validated edge.** Also fixed: nightly intraday retrain crash (orphan `n_workers` kwarg). **NEXT → Track B (owner-gated, at restart):** B1 realized-Sharpe EOD pipeline (the self-certification clock) + B2 Friday cron + B4 aggressive paper-allocation ramp. The event-edge engine (harness + beta-isolation discipline + FINRA/SI + analyst-grades data) is retained for future candidates.

**🟢 TRACK B SHIPPED (2026-06-04):** B1 (realized-Sharpe EOD pipeline) + B2 (Friday rollup) were **already built/scheduled** (16:30 ET, 12 tests) — the self-certification clock is live. **B4 aggressive paper ramp** built + merged: config-driven `pm.pead_size_mult`=3.0 + `pm.pead_max_position_pct`=0.10 (live-tunable via agent_config), PEAD-specific `apply_pead_size_ramp`; RM made PEAD-aware so the 10% per-name cap isn't clipped to the global 5% (aggregate still bounded by the 80% gross cap); ADV-participation instrumentation added (slippage already per-fill). 19 tests; PAPER ONLY. **Deploys at the next uvicorn restart.** Tune the ramp live by editing `pm.pead_size_mult` / `pm.pead_max_position_pct` in agent_config (no redeploy). **B5 SPY<200d trend filter** (2026-06-04) replaces the VIX>30 block — CPCV-validated **+0.661 vs +0.546** (every metric better, same window); wired live config-reversible (`pm.pead_regime_control="trend"`, ma=200), fail-CLOSED to VIX if SPY unavailable. **PEAD now stands down in downtrends (SPY<200d), not just VIX spikes** — protects the ramped book. Deploys at restart.

---

## ☀️ MORNING SUMMARY (2026-06-01) — read this first

**Headline: the honest pipeline killed two illusions and found one real edge.**

| Strategy | Honest OOS CPCV (per-fold, leak-free) | Verdict |
|---|---|---|
| Swing (long-only cross-sectional) | +0.22, t=0.17, 50% pos | ❌ DEAD (noise) |
| Intraday v63 | -2.80, t=-6.85, PF 0.94 | ❌ DEAD (cost-drag); struck fake +5.14 (memorization) |
| **PEAD (post-earnings drift)** | **+0.546, t=2.26, 95% pos, P5 +0.009** | ✅ **REAL EDGE** (clears 0.50 paper gate; short of 0.80) |

**PEAD is the first genuine, statistically-significant, economically-grounded positive result** the project
has produced. Best config: long-only, VIX>30 crisis block ON, k=8, no priced-in filter. The VIX block was
the key lever — it trimmed the crisis-fold left tail (P5 -0.288 → +0.009, %pos 80% → 95%). Mean 0.546 is
short of the 0.80 promotion gate but comfortably clears the 0.50 PAPER gate. PEAD is event-driven (F2-immune),
rules-based (no leakage risk).

### UPDATE (2026-06-01 workday) — PEAD lever sweep + second-edge hunt COMPLETE
All high-EV experiments now run. PEAD config tuning is exhausted; the short-side second-edge hunt failed.

| Experiment | Mean | t-stat | Verdict |
|---|---|---|---|
| **PEAD long-only (baseline)** | **+0.546** | **2.26** | ✅ KEEPER, paper-ready |
| PEAD hold-15 | +0.411 | 1.19 | ❌ killed (drift wants longer hold) |
| PEAD long-short | +0.456 | 2.61 | robust lower-return variant (regime-pass) |
| PEAD earnings-quality | +0.449 | 1.02 | ❌ killed (power collapse) |
| QualityShort shorts-only | **-0.903** | -3.19 | ❌ ANTI-EDGE (old +5.95 was inflated) |

**Conclusion:** PEAD long-only +0.546 is the SOLE validated edge. Config tuning can't reach 0.80 (~0.5-0.7
academic ceiling confirmed). Two short approaches ruled out honestly (inverted-long LX7 +0.036; fundamental-
deterioration QualityShort -0.903) — shorting beaten-down names bleeds (they rally on bounces).

### UPDATE (2026-06-01) — Small/mid-cap PEAD expansion REJECTED (honest result)
Built a survivorship-safe small/mid-cap PEAD harness (Polygon grouped-daily flat files, 8755 delisted names
retained, [$2M,$50M] ADV band, top-300/day, 20bps cost, delisted-haircut wired) — 2 Opus pre-run correctness
fixes (PR #362: haircut no-op + eligibility lookahead), full suite green. **Result: mean +0.361, t-stat +0.95
(coin flip), P5 -1.368 → FAILS, and is WEAKER than R1K large-cap PEAD (+0.546, t=2.26).** Opus oddity review:
REAL failure, result trustworthy (the symbology-gap suspect was disproven — FMP covers the tradeable universe;
low trade count is the shared 5-position cap). The literature's "event edges stronger in small-caps" did NOT
survive honest survivorship + cost modeling. **R1K large-cap PEAD remains the sole edge. Small/mid not worth a re-run.**

**The free-data experiment ladder is now fully exhausted** (swing dead, intraday dead, PEAD levers exhausted,
short edges dead, insider weak, buyback no-data, small/mid-cap rejected). The two decisions below are now the
only remaining moves without new infra/data spend.

### UPDATE (2026-06-02) — Gate recalibrated + PEAD long-only WIRED & ACTIVATED for paper
Both decisions executed (Opus design → 2 review passes each → merged):
- **PR #365 — significance-first two-tier promotion gate** (replaces mean-Sharpe≥0.80 relic). PAPER tier
  (t≥2.0, %pos≥0.75, P5≥0.0, mean≥0.35) + CAPITAL tier (t≥2.5, n_folds≥10, mean≥0.50, explicit sign-off).
  Event-sparsity regime waiver (paper-only, flagged) lets PEAD's `worst_regime_sharpe=None` pass paper with
  `requires_human_review`. **Verdicts (real gate): PEAD → PAPER PASS / CAPITAL HOLD; all else FAIL.** Legacy
  `GATE_MODE=mean_sharpe` is a verified no-op. WF-only runs are INCONCLUSIVE (no longer auto-RETIRE in cron).
- **PR #366 — PEAD long-only wired into live paper** (the live path existed but ran a wrong config). Fixed:
  VIX>30 crisis block now fires (daily VIX series injected; **fail-closed** if unavailable), hold=40 (was 5),
  priced-in filter OFF, scorer pins every validated param. Owner-chosen **risk-managed variant** (keeps
  regime/NIS/opportunity/macro/RM overlays → expected tracking error vs the clean +0.546, logged for
  attribution), **marketable entries** (PEAD-scoped, avoids below-ask adverse selection), **full swing budget**.
  9 `pm.pead_*` config keys (defaults=validated). Observability: `app/live_trading/pead_tracker.py` daily row +
  weekly Sharpe-vs-0.546 rollup email.

**🟢 ACTIVATED 2026-06-02:** `pm.swing_selector` set `pead_quality_short` → **`pead`** (pure long-only; drops
the dead QualityShort anti-edge shorts). **⚠️ REQUIRES UVICORN RESTART** to load the new wiring — run
`.\serve.ps1` (or restart `uvicorn app.main:app`). PEAD begins paper-trading on the next premarket cycle
(08:00 ET analyze → 09:50 ET send). Watch `data/pead_tracking.db` daily rows + the weekly rollup email.
**Capital is withheld** (PEAD is CAPITAL-HOLD pending k≥10 re-run OR live-paper confirmation) — this is paper only.

**🔵 DECISIONS NOW YOURS (autonomous experiment ladder exhausted):**
1. **Paper-trade PEAD long-only** — clears the 0.50 paper gate today (t=2.26, 95% pos, PF 1.54, DSR-pass).
2. **Is 0.80 the right promotion gate?** For a PF-1.54 / 95%-positive / DSR-pass / Calmar-0.77 real edge,
   0.80 + P5>-0.30 may be too strict (multi-LLM reviews flagged it). A deliberate gate decision is warranted.
3. **STOP** long-only price-feature ML (swing+intraday dead) AND fundamental/inverted-long shorts (both dead).
4. **Higher-ceiling future bets** (need new infra/data, not just config): options-PEAD (IV-crush), or the two
   remaining untested shorts (MeanReversion, AnalystRevision momentum) — but LOW priority after 2 short failures.

**Caveats on PEAD:** edge rests on ~8 fold outcomes (N_eff=8); ~15% survivorship upper-bound; verify FMP
`date`=announcement-date PIT (5-min spot check). Solid, economically-grounded, but under-powered.

**9 PRs merged overnight (#335–#348):** save-guard, swing+intraday per-fold retraining, 6 latent integration
bugs (all surfaced only on real runs — mocked tests passed vacuously), daily-source fix (aggregate_5min),
real-data smoke test (#345, guards the empty-matrix class), swing deep-dive doc (#344), PEAD instrumentation
fix (#348, real DSR/regime). Full suite green throughout. notify_watcher auto-starts with uvicorn.

(DEFINITIVE PEAD run with real DSR finishing as of this writing — see logs/p0_pead_cpcv_DEFINITIVE.log;
result will be appended to ML_EXPERIMENT_LOG.)

---

## 🌙 OVERNIGHT PLAN (completed)
All 7 planned items done: intraday definitive (−2.80, dead) → daily-source fixes (#343/#346) → intraday
oddity analysis → real-data smoke test (#345) → PEAD eval (+0.546, the win) → swing deep-dive (#344). Plus
PEAD instrumentation fix (#348) for honest DSR. Process held: branch → Opus implement → Opus review → tests
→ merge → document.

---

## Active Phase
**Honest baseline validation on the fully-corrected pipeline**

All 3 phases of the gate integrity overhaul are merged (PRs #329–#334). Pipeline cleared for CPCV by 3 Opus 4.8 review passes. Now establishing the two honest baselines we've never had: swing v224 CPCV and a clean intraday v63 CPCV re-run (the prior +5.14 predates deployment tracking + the active regime gate).

---

## Model Status
→ See `docs/living/MODEL_STATUS.md` for full details.

- **swing v229** — trained 2026-06-06 (age 4d); XS swing ranker is OFF in the live path (`pm.swing_ml_live_enabled=false`).
- **intraday_meta v65** — trained 2026-06-06 (age 4d).
- **regime v5** — active (age 20d).

---

## RUNNING NOW (2026-05-31): First honest swing CPCV
`logs/p0_swing_v224_cpcv_perfold.log` — swing per-fold CPCV C(6,2), `--per-fold-retrain --as-of 2026-05-29`.
This is the FIRST genuinely out-of-sample ML result in project history. Each fold trains a fresh
model on only its own window (Opus-verified no-leak + per-fold OOS guard, verdict "RUN IT"). ETA ~20-60min.
**The gap between this number and the old frozen +0.08-0.55 swing numbers = the leakage that was inflating them.**
Expect it to be honest-low. A pass (mean Sharpe > 0.80) would be the first promotable swing result.

## Superseded Jobs (2026-05-31, earlier)
1. **CPCV swing v224** (~4h) → `logs/p0_swing_v224_cpcv.log` — first honest swing baseline
2. **CPCV intraday v63 re-run** (~4h) → `logs/p0_intraday_v63_cpcv_postaudit.log` — deployment-adjusted Sharpe on fully-corrected pipeline
3. Macro history + yfinance incremental data updates (~12min)

**Tomorrow:** `/morning` for results → Opus 4.8 analyzes for oddities (esp. if intraday Sharpe stays implausibly high → dig for residual artifacts) → decision tree below.

---

## Decision Tree (after tonight's results)
```
Intraday v63 deployment-adjusted Sharpe?
  > 1.0  → Intraday is REAL edge → lead strategy → begin PEAD as 2nd strategy
  0.3-1.0 → Marginal; the +5.14 was largely a deployment artifact (Opus est. 3-7% real)
  < 0.3  → Artifact confirmed → intraday not deployable as-is

Swing v224 CPCV?
  Passes (mean > 0.80) → run paper-trade prep
  Fails → long-only cross-sectional ranking exhausted (all 9 LX experiments failed,
          incl. LX9-A beta-neutral on corrected pipeline: +0.031, F2=-0.70).
          Next swing work = PEAD (earnings momentum, F2-immune) or proper L/S short model.
```

---

## Strategic Context: Why NOT re-run old models
The pipeline bugs (PF gate non-functional, DSR n_obs=0, pit_union look-ahead, Calmar formula)
affected **measurement**, not the underlying signal. The F2 structural loss (Aug 2024 VIX spike
destroys long-only beta-exposed swing models) is real market behavior — no bug fix changes it.
LX9-A confirms this: beta-neutralized, post-audit, still -0.70 on F2.

**Worth re-running:** only v224 CPCV (never run) + intraday v63 (deployment unknown).
**Not worth re-running:** LX2–LX9, v186–v223 in isolation — failures were F2-structural, not bug-induced.

---

## Data Review (queued for analysis)
After tonight's results, evaluate whether the current data set is the bottleneck:
- Current: yfinance daily/5min, Polygon 5min cache, FMP fundamentals, macro_history, sector ETFs
- Question: are we missing data points that would unlock edge? (options IV/skew, short interest,
  VIX3M term structure, alt data). Opus to assess what's worth downloading vs purchasing.

---

## Blockers / Risks
- Intraday +5.14 Sharpe is implausible for retail equity (daily IR 0.32) — likely a low-deployment
  artifact. Tonight's re-run with deployment tracking will quantify it.
- Swing long-only approach may be structurally exhausted — need forward-looking pivot decision.

---

## Recently Completed
- 2026-05-31: 3-phase gate integrity overhaul merged (PRs #329–#334); 3 Opus review passes; CLEARED FOR CPCV
- 2026-05-31: notify_watcher auto-starts with uvicorn; docs restructured (living/reference/archive)
- 2026-05-31: 13-round adversarial audit complete; PIPELINE_ARCHITECTURE.md is SSOT
