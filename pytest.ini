[pytest]
asyncio_mode = strict
addopts = -n 4 --dist=loadscope --timeout=120
markers =
    e2e: end-to-end integration tests (excluded from normal CI, run with -m e2e)
    slow: slower integration tests (kept under the 120s per-test CI timeout)
